# Арт и анимация — пайплайн, грабли, готовые ассеты

Вынесено из CLAUDE.md при разбиении документации. Текст разделов
перенесён дословно, без правок и сокращений. Карта всех справочников —
в разделе "Карта проекта" корневого CLAUDE.md.

## Art & Animation Pipeline

### Пайплайн (проверенный, воспроизводимый)
```
1. ДИЗАЙН: Nano Banana Pro (Google Flow) → референс-арт персонажа,
   строго вид сбоку ("side view profile, facing right/left"), плашка сзади.
2. АНИМАЦИЯ: Veo (через Flow) → видео 4с/24fps по 4-блочному скелету промпта:
   a) вид сбоку + статичная камера + "never turns toward camera"
   b) кадрирование (wide framing, full body, never touches frame borders)
   c) ДЕЙСТВИЕ — единственный блок, который меняется между анимациями;
      описывать ГЕОМЕТРИЮ движения (что сгибается, куда смещается),
      НЕ эмоцию ("flinches" → модель рисует морду, а не тело)
   d) "Only the character on a flat gray background. No ground, no shadow,
      no dust, no effects" — жёсткий запрет вспышек/пыли/колец на ударе
3. НАРЕЗКА: ffmpeg → кадры → Python (PIL + scipy.ndimage) →
   удаление фона, поиск петли цикла, сборка спрайт-листа + PNG по кадрам.
```
Инструменты нарезки — одноразовые Python-скрипты в чате (не в репо), логика
описана ниже на случай повтора процесса для новых персонажей/анимаций.
Зависимости: `pip install pillow numpy scipy --break-system-packages`.

### Числовая приёмка спрайт-листа (лист не сдан, пока не прошёл)

Проверять **на ДВУХ порогах альфы — 128 и 32**: на 32 дрейф и остатки фона
проявляются сильнее и видно то, что порог 128 скрывает.

Чек-лист (всё измеряется, не смотрится):
- строка ступней (низ фигуры) — разброс близок к нулю;
- альфа в четырёх углах листа ровно **0**, в пустых клетках тоже 0;
- зелёный подмес в полупрозрачных краях **0** — проверка despill:
  у пикселей с `20 < alpha < 220` максимум `G − max(R,B)` должен быть 0;
- нет пустых и продублированных клеток в конце листа;
- рост фигуры и верхняя кромка совпадают с эталоном ±1-2 px.

**Честность про остаточное движение.** Если после единого окна разброс не
нулевой — СНАЧАЛА замерить исходные кадры, а не чинить. Часто это реальное
движение персонажа (переминается, проседает в выпаде): если
`разброс_исходника × scale ≈ разброс_в_листе`, кроп не добавил ничего.
Не «исправлять» такое покадровым сдвигом — это вернёт ровно ту дрожь, от
которой уходили; сообщить цифры и происхождение. Отдельно: критерий
«разброс центра ступней = 0» НЕПРИМЕНИМ к анимациям, где ноги реально
расходятся (выпад, прыжок, широкий шаг) — там медиана гуляет на десятки
пикселей, и это содержание, а не дефект.

**Что отдавать вместе с листом**: размер листа, размер клетки, число
колонок и рядов, число кадров; зациклена анимация или разовая; для атак —
номер кадра с пиком и координаты точки вылета снаряда (центр ладони/когтя)
**внутри клетки** на этом кадре; результаты приёмки числами.

**Проверка глазами — за пользователем.** Контактный лист (все кадры сеткой
на сером фоне) и GIF-превью собираются быстро и решают почти все споры о
том, «нормально ли смотрится»: числа ловят джиттер, обрезку и грязный фон,
но не ловят правдоподобность движения, анатомию после зеркалирования и
читается ли жест.

### Нарезка Veo — правила, не описанные ниже

- **Хромакей применяется ДО кропа, окно кладётся на ПРОЗРАЧНЫЙ холст.**
  Если резать сначала, PIL добьёт выход за границу кадра чёрным, и он
  посчитается непрозрачным объектом.
- **Промпт — типовые поломки и лечение:**

  | Проблема | Причина | Как писать |
  |---|---|---|
  | развернулся к камере | `front-facing` и `profile` в одном промпте — противоречие | только `strict side-view profile` |
  | зеркалится | направление задано абсолютной стороной кадра | `in the direction it is facing` + `do not flip, mirror, or turn` |
  | разворот корпуса | «активные» глаголы `rears back`, `winds up` читаются как поворот | голой геометрией: `pulls arm back behind itself` |
  | наезд камеры | игнор `no zoom` | промптом не лечится — резать первые кадры |
  | отказ по копирайту | силуэт похож на чужую IP | менять пропорции/детали или дать более короткий промпт |

  Всегда добавлять `no text, no watermark, no camera shake, no motion blur`.
- **Число кадров в листе ≠ число кадров в ролике**: 24 fps нужны видео,
  игре хватает 10-16 на цикл, лишнее раздувает текстуру. При разной
  скорости фаз — **неравномерный шаг** (замах каждые 3 кадра, удержание
  каждые 12, возврат каждые 3), а не `linspace`.
- **Диапазон поиска для IoU считать ИЗ ГЕОМЕТРИИ, а не наугад** — на этом
  легко потерять час. Стартовый масштаб `рост_эталона / рост_в_исходнике`,
  стартовое смещение `dx0 = x_лев×scale − x_лев_эталона`,
  `dy0 = y_низ×scale − y_низ_эталона`, дальше перебор ±20 px.
  Здоровый IoU одного персонажа в похожей позе — **0.80-0.87**. Получилось
  0.2-0.4 — почти наверняка диапазон не накрывает верное значение, а НЕ
  «позы разные».
- **Размер клетки — три случая:** один персонаж с множеством анимаций →
  одна общая клетка на все (иначе якорь скачет при смене состояния);
  анимация шире базовой позы → сначала ПРОВЕРИТЬ ЧИСЛАМИ, часто влезает в
  стандартную, а если нет — тот же масштаб пикселей, но более широкий
  холст; разовые VFX → своя рамка под форму, общая квадратная тратит
  память на пустоту.
- **Имя файла при переделке — ВСЕГДА новое**, иначе кеш подхватит старое.

Подробности и код-примеры — `docs/archive/veo-sprite-slicing-SKILL.md`.

### Известные грабли пайплайна (не наступать повторно)
- **Veo даёт секунды статики в начале** видео перед началом действия —
  всегда резать по скачку "движения между кадрами" (diff масок), а не брать
  видео от 1 кадра.
- **Слова "hit/strike/blow/dies/fatal"** триггерят фильтр Flow как опасный
  контент. Обход: переформулировать как физическое состояние без источника
  ("is exhausted and loses his strength" вместо "takes a fatal blow").
- **Модель дорисовывает эффекты, если в тексте есть "причина"** (получил
  удар → чем-то ударили → рисует снаряд/вспышку). Решение — убрать саму
  причину из промпта ("nothing touches it", "there is no ground beneath
  him"), а не только запрещать эффект.
- **Композиция против водяного знака**: знак Flow всегда в правом нижнем
  углу кадра. Если персонаж длинный (четвероногий, лежащий) и дотягивается
  до знака — они сливаются в один объект при вырезании фона. Фикс:
  в промпте явно просить "small in the frame, positioned in the upper
  left, large empty space in the bottom right corner" — тогда знак остаётся
  на пустом фоне и вырезается сам как отдельный компонент.
- **Заливка дыр (binary_fill_holes) не должна быть безусловной**: если
  заливать ВСЕ дыры в силуэте, крупные проймы (между рукой и телом, между
  ног) закрашиваются фоновым серым и остаются видимыми пятнами в игре на
  цветном фоне. Правильно — заливать только мелкие дыры (< ~400 px,
  артефакты контура), крупные оставлять прозрачными.
- **Выравнивание кадров по ступням годится ТОЛЬКО для кадров, сгенерированных
  по отдельности** (напр. спрайт-лист Nano Banana, где персонаж каждый раз
  в новом месте клетки). Для кадров, вытащенных из ОДНОГО видео со статичной
  камерой — кадры уже согласованы между собой; выравнивание по ступням
  каждого кадра отдельно портит анимацию (перса начинает трясти), т.к. якорь
  «ступни» гуляет из-за плаща/лапы/меча внизу силуэта. Правильно: резать все
  кадры одной анимации ОДНИМ неподвижным окном.
- **Между анимациями одного персонажа** (idle/attack/hurt и т.п.) якорь
  нельзя брать по ступням наивно — если в одной из анимаций конечность
  выносится далеко (бросок лапой, выпад мечом), медиана ступней уезжает и
  персонаж прыгает при смене состояния в движке. Правильно — совмещать
  анимации между собой по IoU-наложению силуэтов первого кадра с базовой
  (idle), а не по ступням.
- **Фон не всегда получается чисто зелёным** — иногда Veo даёт зеленовато-
  серый (менее насыщенный) или вообще серый фон (если в промпте забыто
  слово "green"). Простой порог `g > r+15 and g > b+15` в таком случае не
  добирает до alpha=0 в углах (остаётся дымка ~40-50 альфы). Надёжнее:
  сэмплировать РЕАЛЬНЫЙ цвет фона из угла КАЖДОГО кадра и вырезать по
  Евклидову расстоянию до него, а не по фиксированной "зелёности".
- **Разные анимации одного перса, снятые разными роликами Veo, не обязаны
  давать одинаковый bbox** — если в одной анимации конечность вылетает
  далеко за пределы позы из другой (напр. замах когтями шире, чем поза
  Idle), нельзя силой сажать оба в одинаковый квадратный холст: тело либо
  сожмётся мельче на кадрах с широким жестом, либо обрежется. Решение —
  единый МАСШТАБ пикселей (тот же коэффициент src→output, что и у Idle),
  но собственный (более широкий) холст под конкретную анимацию — как уже
  делали для Dash Strike героя (240×128 вместо 128×128).
- **Veo иногда разворачивает персонажа на 180° по горизонтали** несмотря на
  прямой запрет "keep facing direction", причём непредсказуемо — то в
  середине ролика на "активном" глаголе (rears back, атака), то сразу с
  первого кадра. Разворот ловится численно: найти на исходном референсе
  устойчивый асимметричный признак (напр. светящийся глаз) и сравнить его
  положение (% от края bbox) между роликами. Если только силуэт развернут,
  а само движение (напр. замах) логично относительно новой стороны —
  чинится зеркалом (`FLIP_LEFT_RIGHT`) при нарезке. Если же движение
  внутри ролика само по себе идёт "не в ту сторону от головы" (лапа бьёт
  НАЗАД от направления взгляда) — это не лечится зеркалом (оно перевернёт
  и голову, и лапу вместе, сохранив их неверное соотношение) — нужна
  перегенерация с явным "arm moves in the direction the head is facing".
  **Важно:** прежде чем чинить зеркалом — спросить у Андрея, видит ли ОН
  разворот на своём экране. Одного численного анализа недостаточно для
  решения переделывать готовый ролик.
- **"front-facing" и "profile/side-view" в одном промпте — противоречие.**
  Модель может послушать "front-facing" и развернуть персонажа лицом в
  камеру. Для строгого вида сбоку — только "strict side-view profile",
  без слова "front-facing" рядом.
- **Зум-наезд в первые ~5-10% ролика** — отдельный (от разворота) артефакт:
  Veo иногда "наезжает" от уменьшенного кадра до полного масштаба в первые
  8-10 кадров, даже при явном "no zoom" и стартовом изображении-референсе.
  Лечится не перегенерацией, а обрезкой этих кадров при нарезке — ищется
  кадр, где bbox стабилизируется (перестаёт расти), и нарезка начинается
  с него.
- **Референс-изображение "в упор" (объект во весь кадр) провоцирует Veo
  рисовать ещё крупнее и обрезать края.** Перед генерацией видео стоит
  всегда делать "отдалённый" референс-кадр: объект/персонаж на ~35-60%
  высоты холста 1920×1080, с полями со всех сторон, тем же зелёным фоном.
  Правильную долю (35% / 48% / 58% / 62%...) иногда приходится подбирать
  по обратной связи ("сильно отдалил" / "не влезает") — единого числа нет,
  зависит от пропорций объекта.
- **Копирайт-фильтр Google может сработать на силуэт, похожий на чужую IP**
  ("не могу создать видео из-за интересов сторонних поставщиков контента").
  Так сработало на дизайн NPC (сутулая фигура в капюшоне с 2 светящимися
  точками-глазами → похоже на Джаву из Star Wars). Обход — не переделывать
  дизайн полностью, а увести силуэт подальше (рост, форма капюшона,
  асимметричные/другой формы глаза) ИЛИ просто попробовать другой,
  более короткий промпт для видео с тем же PNG (иногда помогает).
- **Зелёный VFX-эффект на зелёном хромакее — плохая идея** (Heal-аура).
  Для таких случаев — чёрный фон + альфа = яркость пикселя (аддитивный
  ключ), а не хромакей по цвету.
- **Обычный экшн-глагол типа "rears back"** может сам по себе читаться как
  "поворачивается" — безопаснее описывать голой геометрией конечности
  ("pulls arm back behind itself"), а не позой всего тела.
- **Тень персонажа отличается от фона всего на ~10-40 по яркости** (почти
  тот же серый), а капюшон/тёмная одежда — на ~70-80. Фильтр "убрать тень
  по разнице с фоном" должен захватывать диапазон (-42..-6), а не любой
  тёмный объект, и применяться ТОЛЬКО к нижней трети кадра — иначе съедает
  тёмные детали костюма (было: капюшон героя лысел в анимации урона).
- **ПРАВИЛО ШИРОКИХ АНИМАЦИЙ**: если персонаж широко машет (руки/меч/выпад),
  кадры собирать на своей ШИРОКОЙ клетке с телом ПО ЦЕНТРУ. Иначе при anchor
  0.5 силуэт обрезается или "улетает" вбок при смене кадра. Так пересобран
  hurt героя (315px шириной вместо 379 у idle/run/attack). ИСКЛЮЧЕНИЕ —
  death: там покадрово НЕ центрируем, падение вбок — часть анимации, не баг.
- **Позиция спрайта в движке завязана на anchor (0.5, 1.0)**:
  `heroSprite.x = centerX бокса`, `heroSprite.y = низ бокса`. Из-за этого
  менять `anchor.x` покадрово НЕЛЬЗЯ (даже если кажется, что так проще
  подогнать смещённый силуэт) — это уводит картинку вбок относительно
  бокса-хитбокса, а не чинит проблему (пробовали на hurt, откатили).
- **Пиксельность/дрожь спрайта в движке** — это НЕ грабля самого арт-пайплайна
  (Nano Banana/Veo/вырезка), а рендер-настройка Pixi: недосэмплирование при
  сильном ужатии текстуры + низкое `resolution` канваса. Фикс — оба сразу:
  `resolution: Math.min(devicePixelRatio, 2)` + `autoDensity: true` на
  `app.init`, И исходник близко по размеру к размеру отрисовки (не 512→140px,
  а 288→140px — см. "Спрайты героя" в разделе Explore).
- **Референс и ролики для Veo — ВСЕГДА на ярко-зелёном фоне** (слово GREEN в
  промпте обязательно; без него Veo даёт серый фон, который плохо режется).
  Референс на зелёном генерить СРАЗУ в Nano Banana, не перекрашивать серый
  готовый арт в зелёный постфактум (даёт паразитную зелёную кайму по контуру).
- **Хвост Veo рисует по-разному в каждом ролике** — все анимации одного
  персонажа с хвостом/длинным придатком надо генерить так, чтобы хвост
  целиком помещался в кадр, и резать в достаточно широкую/ЕДИНУЮ клетку на
  все анимации сразу — иначе хвост не совпадает по месту между roll'ами.
  Эталон хвоста для сверки — walk (там он заметнее всего).
- **Вырезка по зелёному — despill, а не только alpha**: `G = min(G, max(R,B))`
  убирает зелёный "отсвет" на краях силуэта, который порог по alpha не ловит.
  Заливку щелей на спине/между шипами делать ТОЛЬКО в верхних ~60% силуэта
  цветом соседних пикселей — если заливать весь силуэт, получаются: а)
  чёрные просветы на спине на тёмном фоне игры, б) серые пятна в проймах
  между ног.
- **Единая посадка всех анимаций одного персонажа** — самый надёжный способ
  избежать "прыжка" при переключении между ними: одна клетка + голова в
  фиксированной точке (напр. X=130) + ноги на фиксированной линии (напр.
  Y=280) + один и тот же масштаб (высота существа ~243px в клетке 288px).
  Выравнивать по ГОЛОВЕ (стабильный ориентир между кадрами и анимациями), а
  НЕ по "торс-полосе" (шумит от дыхания/наклона) и НЕ по хвосту/лапам (гуляют
  сильнее всего именно они — см. граблю про хвост выше).
- **Дыхательный idle собирать ПИНГ-ПОНГОМ** (полдыхания вперёд + оно же назад
  в кадрах) — иначе на стыке последний→первый кадр виден шов петли. Для
  плавности не жалеть кадров (idle зверя — 24, walk — 16).
- **Death — падение НЕ центрировать покадрово** (смещение тела по ходу
  анимации — часть самого падения, а не баг верстки кадров, см. граблю
  "ПРАВИЛО ШИРОКИХ АНИМАЦИЙ" выше). Выравнивать по клетке нужно ТОЛЬКО
  входной кадр (стартовую стойку) по голове/ногам — остальные кадры кладутся
  в ту же клетку как есть.
- **Цвет между разными генерациями Veo отличается** (один и тот же
  персонаж в разных роликах может быть чуть теплее/холоднее/темнее) —
  приводить все анимации к цвету эталона (обычно idle) гистограммным
  совмещением ПО КАНАЛАМ (R/G/B по отдельности), а не по среднему цвету —
  среднее маскирует сдвиг и не выравнивает реально.
- **Проверять КАЖДУЮ анимацию GIF'ом на тёмном фоне игры на РЕАЛЬНОЙ
  скорости** (не на скорости просмотра/покадрово) до интеграции в код —
  на статичном кадре или медленном превью артефакты цвета/шва/тайминга
  часто незаметны, а на боевой скорости бросаются в глаза сразу.
- **Разъехавшийся якорь между анимациями = прыжок спрайта.** Даже при
  одинаковом масштабе и одной высоте клетки анимации прыгают, если центр
  головы у них на разном X (было: idle 198, hurt 138, jump 152 — разброс
  60px). Лечится пересборкой ВСЕХ анимаций персонажа в одну общую клетку
  с одним якорем (голова по X + ноги по Y), а не подгонкой по одной.
- **Кадры одного ролика нельзя выравнивать покадрово.** Якорь "верх
  силуэта"/"центр головы" гуляет между кадрами (капюшон, шарф, колено) и
  даёт мелькание/тряску. Кадры из ОДНОГО видео сажать ОДНИМ окном:
  единый масштаб + единый сдвиг на всю анимацию.
- **Персонажа в клетке центрировать, не прижимать к якорю idle.** Если
  тело сдвинуть вбок (напр. голова на X=178 в клетке 379), длинный плащ
  спринта вылезает за край и обрезается — обрезанный плащ читается как
  "плащ спереди". Сначала считать габариты силуэта, потом выбирать ширину
  клетки.
- **Despill применять ТОЛЬКО к зелёным пикселям, не ко всему силуэту.**
  G=min(G,max(R,B)) по всему телу утаскивает оливковые ткани в серый
  (было: 8.7% пикселей просели до -56 по зелёному). Правильно — despill
  только там, где G заметно больше R и B.
- **Гистограммное совмещение цвета к другому ролику — опасно.** Попытка
  подогнать цвет нового ролика под старый эталон по каналам дала зелёный
  отлив на тёмном плаще. Если Андрей просит не менять цвет — отдавать
  RGB ролика как есть (байт в байт), править только альфу.
- **Бинаризация альфы порогом даёт "крапинку" на тёмных участках.**
  Тёмный капюшон местами не добирает до порога и становится дырявым.
  Нужна заливка мелких дыр (< ~600px) внутри силуэта; крупные проймы
  (между ног, рука-тело) оставлять открытыми.

### Готовые ассеты — Герой (мечник в капюшоне)
Дизайн: тёмный рваный плащ с капюшоном, лицо скрыто (только янтарные
светящиеся глаза), алый шарф, изогнутый меч с холодным синим свечением.
Смотрит вправо.

| Анимация | Кадров | Loop |
|---|---|---|
| idle | 24 | да, дыхание, пинг-понг |
| run | 21 | да, бесшовно |
| attack | 14 | нет |
| hurt | 10 | нет |
| jump | 24 | нет |
| death | 18 | нет, Loop Time OFF |
| drink | 14 | нет |

Единая клетка на ВСЕ анимации героя — 394×296px, 12 колонок в ряд, перенос
вниз. Общий якорь: голова X=193, ноги Y=282, один масштаб на все анимации —
можно переключать между любыми анимациями без прыжка спрайта. Файлы:
`public/assets/sprites/hero/{idle,run,attack,hurt,jump,death,drink}.png`.

### Готовые ассеты — Враг №1 "Зверь" (четвероногий, ближний бой) — ГОТОВО, ПОДКЛЮЧЕНО
Дизайн: тяжёлый сгорбленный четвероногий монстр, тёмная шерсть, костяные
пластины на спине с тлеющими углями (тёплое оранжевое свечение — контраст
с холодным синим мечом героя), нет глаз, только светящиеся щели. Смотрит
влево (идёт навстречу герою) — базовое направление арта, БЕЗ зеркала.

Финальный набор — единая клетка на ВСЕ 5 анимаций (в отличие от героя, где
у jump/death свои окна): 600×288px, 12 колонок в ряд, перенос вниз. PNG,
зелёный хромакей (вырезан тем же скриптовым пайплайном), quantize 256,
суммарный вес набора ~2MB. Лежат в `public/assets/sprites/beast/
{idle,walk,attack,hurt,death}.png`. Anchor ВСЕХ пяти спрайтов — (0.494, 0.972)
(торс по центру, ноги на низ хитбокса).

| Анимация | Кадров | Loop |
|---|---|---|
| idle | 24 | да, дыхание, пинг-понг |
| walk | 16 | да (используется и в патруле, и в погоне — см. Explore) |
| attack | 24 | нет |
| hurt | 12 | нет |
| death | 18 | нет, Loop Time OFF |

Атака зверя — замах лапой (НЕ укус), цельная анимация (без разбивки на
windup/strike/recover — решили, что не нужна для первой версии кода: урон
привязан к strike-кадру анимации, см. подключение в Explore.tsx ниже).

### Готовые ассеты — Скиллы (VFX)
Все 5 скиллов готовы как спрайт-листы (горизонтальный ряд кадров, PascalCase
имена, папка назначения `public/assets/skills/`). Цвета жёстко привязаны к
дизайн-системе (`right-place-design`): огонь `#F08A24`, лёд `#8FD9F0` (не
путать с кристаллами `#46C4E8`), кровь/danger `#E0353B`, heal/safe `#4FB477`.

| Файл | Кадр (px) | Кадров | Loop |
|---|---|---|---|
| Fireball_Projectile.png | 160×100 | 10 | да, снаряд в полёте |
| Fireball_Impact.png | 160×145 | 12 | нет, взрыв→гарь |
| IceBall_Projectile.png | 160×100 | 10 | да |
| IceBall_Impact.png | 160×145 | 12 | нет, симметричный ледяной "хлопок" |
| Heal_Aura.png | 160×192 | 14 | нет, частицы вверх→гаснут |
| Slash_Streak.png (взмах клинка) | ~широкий, см. чат | 12 | нет |
| Bleeding_Loop.png (капли крови) | узкий/высокий | 8 | да, честный луп капели |
| Dash_Strike.png (герой+дуга ускорения) | 240×128 | 11 | нет |

Целевой размер ОТРИСОВКИ в игре (не размер PNG!) ещё не зафиксирован в коде —
ориентир из чата: снаряды ~56×35px, импакты ~90×82px, Heal-аура ~110×132px,
Slash/Dash ~120-128px высотой (масштаб героя). **Важно:** размер PNG-файла и
размер отрисовки в игре — разные вещи, PixiJS может отрисовать любую текстуру
в любом размере; конкретные числа выше — стартовая точка, не догма.

### Готовые ассеты — Сундук (объект карты)
Два состояния одного объекта, оба выведены из одного и того же исходного
`chest.png` (референс уменьшен на зелёный холст 1920×1080 перед видео):

| Файл | Кадр (px) | Кадров | Событие |
|---|---|---|---|
| Chest_Open.png | 140×178 | 13 | закрыт→замок падает→крышка→золотое свечение |
| Chest_Trap_Explode.png | 190×137 | 14 | закрыт→срыв крышки→взрыв (оранж. `#F08A24`)→гарь |

Оба ролика Veo имели паразитный зум-наезд в первые ~8 кадров — обрезаны
при нарезке (см. граблю выше).

### Готовые ассеты — Контрабандист (NPC, бывш. "Смуглер") — ГОТОВО, ПОДКЛЮЧЁН
ТЕРМИН: в UI и коде теперь "Контрабандист" (не "Смуглер"). Дизайн-канон прежний
(безликий, янтарные глаза, посох, мешок). Спрайт обработан под игру и подключён:
`public/assets/sprites/smuggler/smuggler_idle.png` — 14 кадров, клетка 230×296,
cols=14 (ОБЯЗАТЕЛЬНО 5-й аргумент loadSheetFrames!), anchor (0.5,1.0), loop, idle
дыхание. Смотрит ВПРАВО по умолчанию (спрайт зеркально отражён при обработке):
facing вправо = scale.x положительный. Обработка вырезки: исходник пришёл уже с
прозрачным фоном, но потребовал: premult-ресайз (от каймы), edge-only despill
(от зелени), затемнение внешнего кольца силуэта до цвета контура (от белых
краевых точек — обычный поиск-светлых плодил новые, сработало только force-
darken кольца).

Механика Контрабандиста (Explore, ГОТОВО, офлайн):
- Стоит на карте как объект (по образцу сундука), findGroundSurfaceY, посадка
  SMUGGLER_DRAW_H=148 / SMUGGLER_OFFSET_Y=12. Не стена (проходим насквозь).
- Поворот к игроку: в пределах 4 тайлов (SMUGGLER_TURN_RANGE) флипается лицом
  к игроку; дальше — остаётся смотреть в последнюю сторону (не возвращается в
  дефолт). Дефолт вправо.
- Взаимодействие: игрок близко (SMUGGLER_INTERACT_RANGE=2 тайла) + нажал dodge
  🔄 → перехват dodgePressedRef (обычный dodge гасится) → открывается окно.
- Окно — Pixi Container в worldContainer (ПЕРВАЯ Pixi-интерактивность в проекте:
  eventMode='static' + hitArea Rectangle + 'pointertap'), висит над головой,
  зажато в границах экрана (toGlobal→clamp→toLocal, чтобы не вылетало в углах
  карты). Текст "Контрабандист предлагает обмен / трофеи ×1.5" (риск НЕ пишем),
  2 кнопки "Обменять"(золото 0xE8B23A)/"Уйти"(красный 0xE0353B).
- Обмен: 80% ×1.5 (зелёная прибавка), 20% кража половины (КРАСНАЯ убыль "−N 🏆"
  — spawnRewardFloat получил опц. negative-режим). "Уйти" = закрыть событие без
  обмена (упустить, вариант A). Оба исхода → closeEvent (иконка HUD жёлтая),
  спрайт Контрабандиста остаётся на карте, повторно окно не открыть.
- Трофеи — ЗАГЛУШКА SMUGGLER_TEST_TROPHIES=10 (реальный счёт — Phase 2.5).
  Константы: SMUGGLER_MULT=1.5, SMUGGLER_STEAL_CHANCE=0.20, SMUGGLER_STEAL_FRAC=0.5.

### Готовые ассеты — Иконки экипировки (UI, 6 слотов)
Пустая ячейка слота — векторная панель (не Veo/Nano Banana, собрана кодом
по токенам дизайн-системы: `#221E2B` фон, `#3A3344` рамка). Поверх — тонкий
контур-силуэт предмета (не готовый предмет с текстурой!, просто "иконка
пустого слота" в стиле большинства RPG-инвентарей), приглушённый
лавандово-серый `#9C93AD`, зелёный фон на генерации, вырезан на прозрачность.

| Файл | Слот |
|---|---|
| EquipSlot_Weapon.png | Оружие |
| EquipSlot_Helmet.png | Шлем |
| EquipSlot_Armor.png | Броня |
| EquipSlot_Boots.png | Сапоги |
| EquipSlot_Gloves.png | Перчатки |
| EquipSlot_Amulet.png | Амулет |

Иконки РЕАЛЬНЫХ (заполненных) предметов на все тиры — не делали, для MVP
хватает одной иконки-заглушки на слот.

### Готовые ассеты — Иконки валют (UI)
`public/assets/icons/icon_gold.png` (горка монет), `icon_trophy.png` (череп),
`icon_rp.png` (премиум-монета с огненными RP). Все ~128×128, прозрачный фон,
дарк-фэнтези гритти. ПРЕМИУМ-ЦВЕТ сместился с циана `#46C4E8` на
огненно-янтарный `#F08A24` (форма монеты + горящие RP отличают от золота).
Рисуются мелко (~24px) — см. "Reward float" в разделе Explore выше.

### Готовые ассеты — Загадка-обелиск (объект карты F) — ГОТОВО, ПОДКЛЮЧЕНО
⚠️ Механика полностью переосмыслена с "Puzzle = quiz" на новую систему,
РЕАЛИЗОВАНА в Explore.tsx (заменяет Puzzle на карте F):
- обелиск участвует в рулетке 3 событий забега наравне с остальными
  (`buildEventCandidates` — ОДИН кандидат `kind:'obelisk'` на всё событие,
  точка выбрана `pickRandom` уже на этапе кандидата); если не выпал в
  тройку — на карте его нет вообще;
- выпал: 1 обелиск в `Idle` спавнится в случайной точке из пула кандидатов
  карты (`slots.obelisk?.points` — ОБЪЕКТ с полем `points`, не плоский
  массив, единственное такое поле среди слот-файлов);
- удар по нему → `Burning` + старт события + таймер 30 сек
  (`OBELISK_TIME_MS`) + доспавн ЕЩЁ 3 обелисков в `Idle` из оставшихся точек
  пула (`obeliskCandidatesRef` минус занятая стартовая);
- каждый следующий удар по ещё не сбитому обелиску: +15 сек к таймеру
  (`OBELISK_TIME_BONUS_MS`) — таймер НЕ фиксированный;
- все 4 сбиты → трофеи `rollTrophies(TROPHY_MULT_OBELISK)` (см. "Система
  наград (Explore)" ниже; было золото `OBELISK_REWARD_GOLD_MIN/MAX` —
  константы удалены, золото в забегах больше не выпадает) через
  `spawnRewardFloat` над последним сбитым, `closeEvent(eventIndex)` зажигает
  гнездо HUD, ВСЕ обелиски гаснут обратно в `Idle` и остаются стоять на
  карте как декорация (повторно не бьются — `struck` не сбрасывается,
  остаются стеной по X — `pushPlayerOutX`);
- таймер истёк → смерть героя через СУЩЕСТВУЮЩИЙ путь death (та же
  анимация + `DEATH_HOLD_MS` + abandon, что и от hp≤0 — переиспользуется
  `takeDamage`/`triggerDeath`, отдельной логики смерти нет), событие при
  этом НЕ закрывается (забег и так обрывается);
- иконка гнезда HUD — переиспользован `event_puzzle.png` (отдельной под
  обелиски не делали).

✅ Вопрос "максимальная дистанция между обелисками может не влезть в 30 сек"
(старая заметка ниже, была нерешена) — ЗАКРЫТ добавкой времени за удар
(`OBELISK_TIME_BONUS_MS`): таймер растёт вместе с прогрессом, а не фиксирован.

Дизайн: каменный обелиск без рун (руны просили убрать — "слишком ИИ"). Оба
листа (`Idle`/`Burning`) нарезаны ОДНИМ окном в ОДНОМ масштабе — переключение
между ними (см. `applyAttackHit`/успех события) меняет ТОЛЬКО `sprite.textures`
(тот же `playSpriteAnim`, что у героя/зверя), `anchor`/`x`/`y`/`height`/`width`
пересчитывать НЕЛЬЗЯ — посадка на пол общая, найдена один раз при спавне через
`findGroundSurfaceY` (как у сундука), не пересчитывается от bbox кадра.

| Файл | Кадр (px) | Кадров | cols | Loop |
|---|---|---|---|---|
| Obelisk_Idle.png | 190×512 | 10 | 10 | да, пинг-понг, дыхание трещин |
| Obelisk_Burning.png | 190×512 | 10 | 10 | да, полноценное горение |

Путь: `public/assets/objects/`.

Грабли обработки этих двух листов (доп. к общим граблям хромакея выше):
- в исходниках была зелёная плёнка по всему фону (alpha ~14) и зелёная кайма
  вокруг пламени — вырезано порогом по зелёности `G − max(R,B) ≥ 6` (у камня
  и пламени зелёность ≤ 3, тело/огонь не пострадали);
- `Idle` имел шов петли (кадры 10-11 снова разгорались вместо ровного цикла)
  — пересобран пинг-понгом из первых 6 кадров;
- `Burning` начинался с розжига (кадры 0-1 почти без огня) — в лупе огонь
  гас на каждом цикле; взяты кадры 4-13, розжиг обрезан.

Финальные константы (группа `// Обелиски (карта F)` в `Explore.tsx`):
`OBELISK_DRAW_H=195`, `OBELISK_OFFSET_Y=18`, `OBELISK_HITBOX_W=40`,
`OBELISK_ANIM_SPEED=0.12`, `OBELISK_TOTAL=4`, `OBELISK_TIME_MS=30000`,
`OBELISK_TIME_BONUS_MS=15000`. Награда — `TROPHY_MULT_OBELISK` (см. "Система
наград (Explore)" ниже), не отдельная пара констант — `OBELISK_REWARD_GOLD_
MIN/MAX` удалены вместе с переходом на трофеи.

### Готовые ассеты — Питьё зелья (анимация героя)
Использует `player.png` как референс (меч остаётся в одной руке всё время,
зелье пьёт свободной рукой). Пузырьки/спецэффект от бутылки — обсуждали,
решили НЕ делать, анимации достаточно.

| Файл | Кадр (px) | Кадров | Loop |
|---|---|---|---|
| Potion_Drink.png | 512×512 | 12 | нет, разовое действие |

### Готовые ассеты — Босс (полный набор, подключён)

Босс = эволюционировавший Зверь: двуногий, костяные пластины по рёбрам и груди,
оранжевые угли в трещинах, бледный глаз-щель. Смотрит ВЛЕВО по умолчанию (как
обычный зверь): `facing===-1` без зеркала, `facing===1` — `scale.x` в минус.

Дизайн перегенерён с нуля (старые Boss.png/Boss_Idle/Boss_Melee/Boss_Walk из прошлой
итерации **не используются**). Idle/Walk/Melee/Melee2/Hurt/Death пересобраны на
ЕДИНЫЙ пиксельный масштаб персонажа между собой (см. "Грабли арт-пайплайна —
дополнения" ниже — не далось напрямую из Veo, потребовало полистового тюнера).
**Общей высоты клетки на все листы больше НЕТ** (`BOSS_CELL_H` как одна константа
не существует в коде) — у каждого листа своя клетка ШxВ и своё число колонок.
Якорь тоже свой на каждом листе — И `anchor.x`, И `anchor.y` — ставится вместе со
сменой `textures` в `playBossAnim` (Explore.tsx), внутри одной анимации не меняется.
Ranged — арт подключён, логика дальней атаки реализована (ФАЗА 3, см. ниже);
лист по-прежнему НЕ пересобран в единый пиксельный масштаб персонажа, но
нормализован ПОДБОРОМ поверх этого: `BOSS_SCALE_FIX_RANGED=1.00` домножает
базовый scale, `BOSS_ANCHOR_X_RANGED=0.593`/`BOSS_ANCHOR_Y_RANGED=0.985` —
якорь (см. таблицу и примечание под ней). Stomp — арт готов, но НЕ
нормализован, ждёт фазы 4.

| Файл | Клетка | Кадров | cols | anchor (x, y) | Тип / ключевой кадр |
|---|---|---|---|---|---|
| Boss_Idle.png | 187×287 | 24 | 12 | (0.4064, 0.9826) | цикл, **пинг-понг** (вдох не сходится) |
| Boss_Walk.png | 231×283 | 24 | 12 | (0.4719, 0.9788) | цикл вперёд |
| Boss_Melee.png | 345×345 | 24 | 11 | (0.5391, 0.9855) | разовая, **удар на кадре 9** |
| Boss_Melee2.png | 679×469 | 37 | 5 | (0.6127, 0.8380) | разовая, **удар на 19**, двойная дуга |
| Boss_Hurt.png | 220×286 | 31 | 12 | (0.4818, 0.9825) | хитстан, возврат в стойку |
| Boss_Death.png | 355×298 | 46 | 11 | (0.5183, 0.9329) | разовая, **последний кадр = труп** |
| Boss_Ranged.png | 297×328 | 24 | 12 | (0.593, 0.985)* | шип в руке **с 11**, **выпуск на 20** |
| Boss_Stomp.png | 298×378 (сверено по заголовку PNG) | 24 | 12 | — **НЕ нормализован**, ждёт фазы 4 | разовая, **удар о землю на 7** |
| Boss_Spike.png | 200×67 | 1 | — | — | снаряд, острие влево, без вращения |
| Boss_Spike_Impact.png | 347×369 | 20 | 10 | — | попадание: раскол→падение→затухание |
| Boss_Wave.png | 280×153 | 8 | 8 | — | AoE-волна, обе дуги в одном спрайте |

\* Boss_Ranged.png нормализован ПОДБОРОМ (живым тюнером, тюнер убран из
кода), не пересборкой листа — `BOSS_SCALE_FIX_RANGED=1.00` домножает
базовый scale, якорь ставится через отдельные константы
`BOSS_ANCHOR_X_RANGED`/`BOSS_ANCHOR_Y_RANGED` (не через общий
`BOSS_ANCHOR_X`/`BOSS_ANCHOR_Y` Record, как у остальных листов). ⚠️ Якорь по
X заметно смещён от центра (0.593, не ~0.5) — фигура на листе нарисована со
сдвигом. Из-за этого любая точка на спрайте (например, точка вылета шипа,
см. `spawnBossSpike`) считается через `sprite.anchor`, а НЕ от
геометрического центра клетки.

Пути: `public/assets/sprites/boss/`, снаряд — `objects/`, эффекты — `vfx/`.
`Boss_reference_green_1920.png` — зелёный референс (босс 50% высоты холста),
**стартовое изображение для всех будущих роликов Veo по боссу** (держит единый масштаб).

#### Механики босса (согласовано, НЕ переспрашивать)
- **Ближний бой — две атаки**: Melee (быстрый росчерк одной лапой наотмашь),
  Melee2 (тяжёлый двойной удар с полумесяцем). Урон на кадре удара, не по нажатию.
- **Дальний бой — ФАЗА 3 ЗАВЕРШЕНА** (константы/детали — см. "Босс (карта C) —
  ФАЗА 3" ниже). ⚠️ Решение ИЗМЕНЕНО против исходной записи здесь ("от бедра,
  низко, траектория прямая горизонтальная") — реальная анимация Boss_Ranged даёт
  ВЫСОКИЙ бросок (кисть на кадре выпуска ~200px над землёй и по ходу ролика
  только поднимается, низкого кадра с вытянутой рукой на листе нет), прямой
  горизонтальный шип пролетал НАД головой героя. Заменено на баллистическую дугу
  с прицеливанием в центр героя на момент броска, шип разворачивается по вектору
  скорости (не зеркалится). **1–2 шипа за атаку рандомно** (второй = повтор
  анимации без возврата в стойку). Импакт обязателен — и на попадании в героя, и
  на попадании в пол (шип может промахнуться дугой). Уклонение: отход **и**
  кнопка dodge.
- **AoE (топот)**: удар на кадре 7 → волна **в обе стороны**, урон
  `BOSS_WAVE_DAMAGE=16` за касание, уклонение **только прыжком**, волна
  катится с задержкой (телеграф) до игрока или до препятствия. ⚠️ Комментарий
  у `BOSS_STOMP_SRC` в `constants.ts` (район строки 131) устаревший — говорит
  "без волны/урона", хотя `spawnBossWave`/`deps.takeDamage` в `boss.ts`
  реально подключены; не переносить формулировку оттуда.
- **Death**: оседает на колени → валится набок, тело **остаётся лежать до конца
  забега** (в отличие от обычного зверя, который despawn'ится).

#### Не сделано по боссу
- Melee2 не разделён на «босс без дуг» + «дуги отдельным VFX»: дуга и тело в одном
  слое, в местах наложения второй дуги на руку остались тёмные пятна (принято как
  компромисс). Идеальный путь — перегенерить ролик без дуг, дуги отдельно (как сделано
  для ranged: босс отдельно, шип отдельно).
- Melee2 суммарно растянут ×1.62 от исходника Veo (нормализация масштаба между
  листами, см. грабли ниже) — будет заметно мягче/размытее прочих анимаций босса.
  Известный компромисс, лечится только перегенерацией ролика, не постобработкой.

---

### Грабли арт-пайплайна — дополнения (выстраданы на боссе)

**Хромакей (итоговый конвейер вырезки):**
1. Цвет фона — медиана по 4 углам КАЖДОГО кадра.
2. **Альфа считается кейером по зелени**, а НЕ по евклидовой дистанции до фона:
   `α = 1 − (G − max(R,B)) / (то же у фона)`. Дистанция завышает α для светлых
   полупрозрачных элементов (следы, дым) — они остаются зелёными.
3. Цвет восстанавливать размериванием: `C = (наблюдаемый − (1−α)·фон) / α`.
4. **Замена цвета на «ближайший чистый» — ТОЛЬКО в полосе ≤4px от края.** Если гонять
   по всему телу, внутренние детали (зубы, язык) перекрашиваются в цвет соседей —
   пятна. Именно так я испортил пасть, а потом «лечил» её перекраской — двойная ошибка.
5. **Зелёные зоны в глубине тела** (фон виден сквозь раскрытую пасть) — заливать
   **фиксированным** тёмным цветом глотки с мягким краем. Фиксированный = одинаковый
   на всех кадрах = без мерцания.
6. **despill-формула `G=min(G,max(R,B))` на ВСЮ заливку запрещена** — краевой пиксель
   (смесь зелёного фона с тёмным контуром) превращается в тёмную бирюзу. Внутри тела
   формула безопасна.
7. Ужатие — **только с premultiplied alpha** (premultiply → Lanczos → unpremultiply).
   Без этого цвет прозрачной зоны затекает в край и создаёт светлую кайму заново,
   сколько маску ни чисти.
8. Заливать **только мелкие** дыры. Крупные проёмы (между лап) заливать нельзя —
   станут чёрным клином. Исключение: **верхние ~55% силуэта** (пасть) — там порог дыр
   поднимать до ~9000px, иначе раскрытая пасть мигает дырой на фон.
9. Порядок: угрозы → потом отсев отвалившихся островков. Наоборот — угрозы наречет
   новые обрывки прядей, и они повиснут точками вокруг силуэта.

**VFX (следы, дуги, пыль):**
- Тело — **жёсткий край**; VFX — мягкая альфа. Смешивать нельзя: мягкая альфа на теле
  = светлая кайма по контуру.
- VFX отделяется по **удалённости от силуэта** (>3px). Пиксели вплотную к телу к VFX
  не относить — иначе белая кайма вокруг персонажа.
- **Когда VFX пересекает тело** (дуга проходит сквозь руку/ноги) — правильного
  однослойного решения НЕТ: либо тёмные пятна в дуге, либо разрезанные конечности.
  Решение — генерить ролик БЕЗ эффекта и класть эффект отдельным ассетом.
- Пыль/дым можно сохранить как полупрозрачный слой — тогда нет дыр и черноты в местах,
  где эффект лёг поверх тела.

**Никаких правок «поверх» вырезки:**
- **Любая покадровая перекраска «по ближайшему» даёт рябь** — источник замены меняется
  от кадра к кадру. Внутрь силуэта не лезть вообще.
- Заморозка детали (перенос морды с опорного кадра) работает только при чистом
  СОВПАДЕНИИ. Если голова есть и поворачивается — лезет призрак старого лица.
  При вклейке менять **только цвет**, альфу не трогать (иначе куски висят в воздухе).
- Временное сглаживание по соседним кадрам мылит деталь и проблему не решает.

**Промпты Veo — что реально работает:**
- **Дрожание рисунка обработкой не лечится, только промптом.** Если Veo каждый кадр
  перерисовывает деталь (челюсть), нужен прямой запрет: «jaw stays completely still,
  the mouth keeps exactly the same shape, the face does not move».
- **Прилагательные веса («heavy», «powerful») в описании атаки Veo отыгрывает как
  подготовительную фазу** (лишний замах двумя руками), а не как вес удара. Нужна
  скорость — писать «quick», вес добиваться позой.
- **Скорость и амплитуды разводить как отдельные параметры**: «quick but small».
  Одним словом получается либо рывок, либо вяло.
- **Ускорять анимацию прореживанием кадров нельзя** — рябь. Медленный ролик =
  перегенерация, а не каждый второй кадр.
- **Перечислять детали дизайна вредно**: подробный список брони модель понимает как
  «дорисовать больше». Работает короткое «looks exactly like the reference image in
  every frame — nothing added, nothing removed».
- **Убирать причину, а не запрещать следствие**: «throws» тянет за собой предмет в
  руке, «slams onto the ground» — пыль. Помогает описывать голую геометрию движения
  («swings its arm forward at waist height») и «nothing below it, empty space».
- **Длинный промпт = длинное действие** (лишние повторы). Для одиночного движения —
  3–5 строк и явное «only ONE, it must not repeat».
- **Циклы (walk/idle)**: период искать **перебором пар начало-конец**, а не «ближайшим
  к нулевому кадру». Проверять глаз числом относительно среднего шага (цель <1.0).
- **Отдалённый референс обязателен**: объект «в упор» заставляет Veo наезжать и резать
  край. Для босса 50% высоты холста, для мелких объектов ~25% ширины.

**Проверка результата:**
- Смотреть **ВСЕ кадры теркой**, а не 5–7 выборочных. Половина ошибок пряталась именно
  в непроверенных кадрах.
- **Числовые метрики врут** — дважды показывали «улучшение» там, где глаз видел брак,
  и «без изменений» там, где стало хорошо. Приёмка только визуальная.
- **Превью-GIF собирать с ЕДИНОЙ палитрой** на все кадры (MEDIANCUT по склеенным
  кадрам). `ADAPTIVE` на каждый кадр = цвета плавают = ложная «рябь», которой в PNG нет.
- Переделанные файлы отдавать **под новым именем** — иначе кеш показывает старую версию.
- **Документации о наличии ассетов на диске верить нельзя — проверять листингом папки.**
  Реальный случай: этот файл утверждал, что VFX-арт всех 5 скиллов готов и лежит в
  `public/assets/skills/` (раздел "Готовые ассеты — Скиллы (VFX)"), а по факту на
  диске лежали только старые строчные заглушки `Battle.tsx`, PascalCase-файлов не
  было вообще (см. "Skills" выше). Расхождение вскрылось только когда перед
  удалением `Battle.tsx` прогнали `ls` по папке, а не поверили записи в документе.
- **Veo меняет отдалённость камеры между роликами** — заявленный единый масштаб
  персонажа между листами НЕ гарантирован, даже если так написано в README набора.
  Проверять обязательно. Численные метрики масштаба ВРУТ: высота силуэта завышена
  вытянутыми лапами и дугами удара, площадь занижена ракурсом, ширина головы зависит
  от поворота — все три давали разные ответы на одном наборе. Единственный рабочий
  способ — временный тюнер с полистовым множителем масштаба, разработчик подгоняет
  глазами по голове. Итоговые множители босса: melee ×1.08, melee2 ×1.20, death ×1.08
  поверх нормализации по ширине головы.

**Грабли, выстраданные на сцене "убежище" (герой у костра, вкладка Исследовать):**
- **Исходник для Veo делать СРАЗУ в 16:9 (1280×720).** Квадратный исходник Veo
  вписывает в широкий кадр обрезкой — у костра срезало языки пламени в 81
  кадре из 96.
- **Veo обесцвечивает персонажа относительно эталона.** Лечится не перекраской
  на глаз, а замером среднего и разброса по цветовым каналам на пикселях
  персонажа и переносом статистики с эталонной картинки (коррекция по
  каналам, не по общему тону). У героя коэффициенты вышли 1.61 / 1.40 / 1.20
  по R/G/B.
- **Ширина спрайт-листа не должна превышать 4096px** — предел текстур
  мобильных GPU. Если кадров много (герой — 24), раскладывать СЕТКОЙ
  (6 колонок × 4 ряда), а не одним длинным рядом.
- **Отрицательный z-index уводит элемент ЗА фон родителя**, а не «между фоном
  и контентом», как может показаться. Фон нужно выносить отдельным слоем с
  явным неотрицательным `zIndex` (0), а не пытаться просунуть контент под
  него отрицательным индексом.
- **Линейная анимация `background-position` НЕ сглаживает спрайт-лист** —
  вместо смены кадров получается непрерывный сдвиг картинки вбок (кадры
  "едут", а не переключаются). Спрайт-листы анимировать только `steps()`.
- **Плавность анимации задаётся частотой кадров (числом кадров в исходнике),
  а не длительностью CSS-анимации.** Длительность меняет только скорость
  прохода по уже существующим кадрам, но не добавляет промежуточных поз —
  «дёрганость» лечится перегенерацией с большим числом кадров, не тюнингом
  секунд.

### Грабли генерации иконок (сессия 31.08.2026)

Всё выше в этом файле — про спрайт-листы анимаций из Veo. Ниже — отдельный
случай: статичные иконки предметов, другой генератор и другие грабли.

- **Хромакей выедает предметы своего цвета.** Зелёная печать хила и зелёная
  лента свитка ушли в прозрачность, у иконки остался дырявый центр. Лечится:
  фон определять не порогом «избыток зелени», а заливкой от краёв кадра
  (`ndimage.label` + связность с границей) — всё, что внутри силуэта, объект
  по определению. Плюс ужесточить порог до `G > R+60 & G > B+60 & R < 110 &
  B < 110`. Контроль: сравнить число непрозрачных пикселей между иконками
  одной группы. Расхождение в 1.5–2 раза = предмет выеден. На финальном
  наборе разброс внутри групп вышел 1.01 — метод работает.
- **«each item fits in a square» в промпте → генератор рисует квадратные
  плашки за предметами.** Про пропорции нельзя писать слово square.
- **Отрицания генератор глотает** (`no black outline` не работает). Работает
  позитивная формулировка: `edges formed by the painted material itself`.
  Для фона — перечислять конкретные формы: `no frames, no panels, no
  plaques, no borders`.
- **Обрезку краёв проверять ИЗМЕРЕНИЕМ, а не глазами.** На листе печатей
  показалось, что 5-й диск срезан — измерение дало поля 37px слева и справа,
  ровно симметрично. Впечатление обмануло.
- **Вытянутые предметы (свитки, 1:3) в квадратной ячейке превращаются в
  ниточку.** Лечится поворотом на −35° перед вписыванием: ширина выросла с 41
  до 85px при высоте 120. Поворот делать по premultiplied alpha, затем
  перекадрировать по силуэту.

### Не хватает по арт-пайплайну
- Иконки РЕАЛЬНЫХ предметов экипировки (не заглушки пустых слотов) — на
  будущее, после того как определятся конкретные предметы/тиры.
- Разбивка атаки зверя (обычного) на windup/strike/recover — сделана, но
  не решено, нужна ли она в первой версии кода.

---

