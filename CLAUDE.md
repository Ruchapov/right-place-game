# Right Place — Telegram Roguelike

## Карта проекта

Подробности вынесены в `docs/`. Читать их ЦЕЛИКОМ не нужно и вредно —
открывай точечно тот файл, который относится к текущей задаче. В этом
файле остаётся только то, что нужно в любой сессии.

| Файл | Строк | Что внутри |
|---|---|---|
| [`docs/explore-engine.md`](docs/explore-engine.md) | 868 | физика, коллизия, камера, AI зверя и босса, хитбоксы, HUD-плита, управление |
| [`docs/explore-features.md`](docs/explore-features.md) | 175 | броня, зелье, сундук, reward float, формула трофеев |
| [`docs/entity-modules.md`](docs/entity-modules.md) | 286 | контракт createXSystem, порядок вызовов в тикере, skills/enemy/boss |
| [`docs/skills.md`](docs/skills.md) | 361 | пять скиллов, восстановленная из git реализация Battle.tsx, арт |
| [`docs/server-explore.md`](docs/server-explore.md) | 189 | эндпоинты забега, розыгрыш событий на сервере, экраны итогов |
| [`docs/maps.md`](docs/maps.md) | 252 | шесть карт A–F, слот-файлы, декор, темы, тайник D, обелиски F |
| [`docs/items.md`](docs/items.md) | 340 | каталог 36 предметов, тиры, слоты, инвентарь, дроп (спроектирован, не реализован) |
| [`docs/shop.md`](docs/shop.md) | 161 | разделы, вёрстка витрины, зелья, цены, что не сделано |
| [`docs/meta-screens.md`](docs/meta-screens.md) | 202 | экраны Персонаж/Снаряжение/Друзья/Исследовать, иконки, стиль |
| [`docs/art-pipeline.md`](docs/art-pipeline.md) | 654 | генерация и нарезка спрайтов (Nano Banana/Veo/хромакей), грабли, список готовых ассетов |

## Project Info
- **Bot:** @RightPlaceGame_bot | **Mini App:** t.me/RightPlaceGame_bot/game
- **GitHub:** https://github.com/Ruchapov/right-place-game
- **Frontend:** https://ruchapov.github.io/right-place-game/ (GitHub Pages)
- **Server:** https://right-place-game.onrender.com (Render, Frankfurt, Free)
- **Dev:** Andrey Rychapov (solo) | **Working dir:** D:\dev\telegram-game

## Mentor Rules
- Beginner solo dev. ONE step at a time. Explain WHAT/WHY, then HOW to verify.
- Commands for **cmd** (not PowerShell). Russian is fine.
- When broken: ask for actual error before guessing.
- Design decisions in chat; Claude Code applies edits in terminal.
- Never give full file replacements — give precise prompts for Claude Code (anchored edits).
- Always: plan full feature first → confirm → one step at a time → commit → deploy → test on phone → wait for confirmation.

## Tech Stack
- Frontend: React 19 + TypeScript + Vite 8 → GitHub Pages
- Game engine: PixiJS v8 (combat scenes)
- Telegram SDK: @telegram-apps/sdk v3
- Backend: Node.js v24 + Fastify 5 + TypeScript (ESM) → Render
- DB: PostgreSQL (Neon, Frankfurt) + Prisma ORM v5
- Auth: Telegram initData verification + JWT (7d)

## Key Commands
```
# Frontend
npm run dev -- --host     # dev server, ROOT, port 5173
npm run deploy            # deploy to GitHub Pages (hard-reload Mini App after!)

# Server
cd server && npm run dev  # tsx (NOT ts-node), port 3000
git push                  # triggers Render auto-deploy (sometimes needs Manual Deploy)
```

## Critical Gotchas
- **Server dev runner is `tsx`** — `ts-node --esm` had Windows ERR_MODULE_NOT_FOUND bug.
- **Server imports need .js extension**: `from '../auth.js'` not `from '../auth'` (ESM)
- **Two auth.ts files**: `server/src/auth.ts` (Telegram verify) vs `server/src/routes/auth.ts` (login route)
- **Run server from server/**: prompt must read `...\telegram-game\server>`
- **After `npm run deploy`**: `•••` → Reload Page in Telegram (aggressive caching)
- **Render auto-deploy sometimes doesn't fire** → Manual Deploy → Deploy latest commit
- **Prisma `migrate dev` reset prompt**: NEVER auto-confirm — already wiped test data once.
- **Open as Mini App** (t.me/RightPlaceGame_bot/game) — initData only works in Telegram, NOT in browser
- **UI-отладка теперь и через `localhost:5173/right-place-game`** (`npm run
  dev -- --host`, hot-reload): `App.tsx` при отсутствии/ошибке
  `retrieveRawInitData()` (т.е. вне Telegram) подставляет заглушку-игрока
  `DevTester` и НЕ ходит на сервер — быстро проверять вёрстку/расположение
  HUD без телефона. Финальная проверка серверной логики и реального
  HP/забега — ТОЛЬКО в Telegram (initData не работает в браузере).
- **VPN blocks localhost** — disable during local dev
- **Port 3000 busy?** → `taskkill /F /IM node.exe` then restart server
- **RUN_COST = 3** in `server/src/routes/run.ts` — keep 3 in dev, restore to 10 before release
- **Render sometimes deploys old commit** → always check Events tab after push
- **`git push` деплоит ТОЛЬКО сервер** (Render автослушает пуш). Фронт на
  GitHub Pages деплоится ОТДЕЛЬНО, `npm run deploy` — на этом дважды теряли
  время за сессию, думая, что фронт уже обновился после одного `git push`.
- **Применённую Prisma-миграцию НИКОГДА не редактировать задним числом.**
  Prisma отмечает миграцию по ИМЕНИ папки в `_prisma_migrations`, содержимое
  `migration.sql` после применения не перечитывает — правка уже
  задеплоенного файла молча разойдётся с тем, что реально накатано на
  проде, и следующий `migrate deploy` упадёт с P2022 (колонка/данные не
  совпадают с тем, что Prisma думает уже применено). Нужно другое
  состояние схемы/данных — НОВАЯ миграция поверх, не правка старой.
- **Порядок деплоя при изменении схемы БД**: сначала `cd server && npx
  prisma migrate deploy` (или `migrate dev` локально) против целевой БД,
  ПОТОМ `git push`/`npm run deploy` кода, который на эту схему рассчитан.
  Обратный порядок — рабочий код, ждущий колонку/данные, которых ещё нет.
- **Перед ЛЮБОЙ диагностикой "не работает" — сначала проверить, что реально
  развёрнуто** (какой коммит на Render, обновлялся ли GitHub Pages, см.
  два пункта выше). Характерный симптом путаницы со старым фронтом: сервер
  честно отвечает 200 с правильными данными, а в Telegram ничего не
  меняется — потому что открыт старый закэшированный клиент, а не потому
  что сломан сервер.
- **Перед деплоем сервера** прогонять `python tools\check_map_sync.py` —
  сверяет `public/assets/maps/*_slots.json` с ручной копией
  `server/src/maps/*_slots.json` по sha256 (read-only, ничего не чинит
  сама). Копии реально расходятся, если карту правили только на одной
  стороне.
- **Высоту контейнера под фоновую картинку задавать ЯВНО** (`calc()` от
  известной ширины, ИЛИ `padding-bottom: <aspect>%` на обёртке) —
  auto-высота от `<img>` не даёт родителю определённую высоту, и проценты
  `top`/`height` у абсолютно спозиционированных детей внутри считать не от
  чего (тихо превращаются в 0/auto). Наступали на это ДВАЖДЫ в почти
  идентичной форме: HP-плита (`HP_FRAME_H`) и плита итогов забега
  (`results_plate.png`, паддинг-боттом трюк) — оба раза чинилось одним и
  тем же приёмом.
- **Удаление колонки в проде — ВСЕГДА две миграции, не одна.** Сначала
  `add` (новые колонки + переливка данных), выкладка нового сервера,
  проверка вживую — и только отдельным заходом `drop`. Одной миграцией
  нельзя: Prisma перечисляет колонки в `SELECT` поимённо, а деплой на Render
  не мгновенный, поэтому ещё живой старый процесс начинает падать на каждом
  чтении персонажа, то есть на ЛОГИНЕ, у всех сразу. Проделано на
  `potionCharges` → `potionT1..T5` (`20260911120000_potion_tiers_add` +
  `20260911130000_potion_charges_drop`).
- **Папку `drop`-миграции нельзя создавать заранее, «чтобы была».**
  `prisma migrate deploy` применяет ВСЕ pending-миграции разом, так что
  заготовленная папка сработает на ближайшем же деплое и уронит логин ровно
  так, как две миграции и должны были предотвратить. Создавать её — вторым
  заходом, непосредственно перед применением.
- **Шипы vs габарит игрока: инвариант, который перевернётся МОЛЧА.** Урон
  шипов меряется по ЗАМЕРЕННОЙ макушке спрайта
  (`SPIKE_BODY_TOP_GROUND = 3.73` / `SPIKE_BODY_TOP_AIR = 31.85`,
  `constants.ts`), а не по верху бокса `phys.y` — бокс выше настоящей головы,
  и разброс зависит от позы (в прыжке ноги поджаты, якорь смещён, см.
  `RISE_ANCHOR_Y`/`FALL_ANCHOR_Y`). Отсутствие бага держится на неравенстве
  `JUMP_HIT_OFFSET_Y − TILE_SIZE*PLATFORM_H_RATIO − SPIKE_BODY_TOP_AIR <= 0`,
  сейчас `40 − 28.16 − 31.85 = −20.01` (запас 20px). Сменят спрайт героя или
  поднимут высоту прыжка — знак перевернётся, и шип на узкой платформе `'='`
  снова начнёт бить по воздуху над головой, БЕЗ падений и ошибок. Рецепт
  перезамера обеих констант — в комментарии к ним в `constants.ts`.
- **Анти-чит-потолки ломаются молча при смене формы данных.** Потолок
  полученного урона считался как `run.potions * 0.25`, где `potions` был
  скаляром. При переходе на массив по тирам выражение не упало бы — оно
  дало бы `NaN`/мусор и начало РЕЗАТЬ рост выносливости у честных игроков.
  Пересчитано на `run.sips × максимальный healFrac по каталогу`. Правило:
  меняя форму поля в `currentRun`, отдельно проверять все анти-чит-формулы,
  которые его читают.

## Security TODO (before real users)
- Rotate JWT_SECRET (weak placeholder) and Neon DB password

---

## Architecture

### Server (server/src/)

Полный список (server/src/routes/run.ts — 6, + auth.ts + index.ts — сверено по факту, не по памяти):

| Endpoint | Body | Description |
|---|---|---|
| `POST /auth/login` | `{initData}` | Verify Telegram initData → JWT; закрывает зависший explore-забег как смерть (`interruptedRun`, см. Phase 2.5) |
| `POST /run/start-explore` | `{mapFile?}` | Explore: карту выбирает сервер, если `mapFile` не прислан; см. Phase 2.5 |
| `POST /run/finish-explore` | `{closedEvents, died, smugglerOutcome?, attackDamageDealt?, skillDamageDealt?, healedAmount?, damageTaken?, potionsDrunkByTier?}` | Explore: начисляет/обнуляет трофеи, закрывает currentRun; см. Phase 2.5 |
| `POST /character/skills` | `{skills: string[]}` | Save equipped skills (max 2) |
| `POST /character/buy-potion` | `{tier}` | Покупка ОДНОГО зелья тира 1..5. Цена и `levelRequired` — из каталога (`server/src/potions.ts`), не из тела. Ответ `{gold, potions[5]}` |
| `GET /character/inventory` | — | Список предметов персонажа. ⚠️ Текущий UI вкладки "Снаряжение" на хардкоде и этот эндпоинт не зовёт (см. "Система предметов" ниже) |
| `POST /character/equip` | `{inventoryItemId, equip}` | Надеть/снять (проверка уровня, авто-снятие того же слота). Тот же ⚠️, что выше |
| `GET /health` | — | `{status, game, timestamp}` |

**Prisma Character fields** (сверено по `server/prisma/schema.prisma`, без id/userId/createdAt/updatedAt/relations):
`level, energy, lastEnergyUpdate, endurance, strength, agility, luck, strengthProgress, enduranceProgress, agilityProgress, bonusLevels, gold, trophies, crystals, potionT1..potionT5, equippedSkills([]), currentRun(Json?)`.
Скалярного `potionCharges` БОЛЬШЕ НЕТ — заменён пятью `potionT<N>` (Int, `potionT1` default 3, остальные 0), колонка удалена миграцией `20260911130000_potion_charges_drop`.
`bonusLevels` (Int, default 0, `schema.prisma:38`) в прежней версии этой записи ОТСУТСТВОВАЛ — а он живой: это единственный источник уровней НЕ от статов, его читает `calculateLevel(strength, agility, endurance, bonusLevels)` и в `/run/start-explore`, и в `/character/equip`. Полей `totalDamageReceived`/`totalDamageDealt`/`totalSkillUses` в схеме НЕТ (были в старой версии этой записи, не сверялись).
⚠️ **`luck` и `crystals` нигде не читаются и не пишутся в `server/src`** (сверено grep'ом по всему серверу) — открытый вопрос, не задача: это не следствие удаления старого потока, поля были мёртвыми и до него.

**`currentRun` shape** — один вид, старого 3-комнатного потока больше нет:
`{ mode:'explore', mapFile, events, hp, maxHp, potions }` (Phase 2.5) — `events` несёт полный розыгрыш (trophyReward/isMimic), клиенту наружу не идёт целиком (см. Phase 2.5)

### Frontend (src/)

`Battle.tsx`, `Smuggler.tsx`, `Puzzle.tsx` — старый 3-комнатный боевой поток
(PixiJS-сцена боя + окна смуглера/загадки старого потока) — УДАЛЕНЫ целиком
(см. Next Steps, [СТАРЫЙ ПОТОК]).

**`App.tsx`** — Main state.
PlayerData: `{id, firstName, level, gold, trophies, strength, endurance, agility, equippedSkills, potions[5]}`
- Navigation: 5-tab bottom nav (Персонаж / Магазин / Исследовать / Снаряжение / Друзья)
- Кнопка "Начать забег" открывает `<Explore>` БЕЗ `mapFile` — карту
  называет сервер (Phase 2.5, см. ниже). Debug-панель карт A-F передаёт
  `mapFile` явно и продолжает работать как раньше.

---

## Stat & Leveling System

Полностью переписано (см. `server/src/game.ts`) — старая экспоненциальная
нормализация урона по уровню (`base*growth^(stat-10)`,
`normalizeDealt/ReceivedDamage`) и старый инкрементальный левелинг
(`checkStatLevelUp`, `LEVELUP_*_GAIN`, поля `strengthAtLevelUp`/
`enduranceAtLevelUp`, скрипт `fix-level-snapshots.ts`) — УДАЛЕНЫ, в коде и
в схеме их больше нет.

### Growth (IMPLEMENTED) — `applyStatProgress`, `game.ts`
Порог до следующей единицы стата растёт от самого стата (не от уровня):
```
порог(стат) = round(база * (стат/10)^STAT_THRESHOLD_EXPONENT)
STAT_THRESHOLD_EXPONENT = 1.2
```
База своя на канал: `STRENGTH_THRESHOLD_BASE=710`,
`ENDURANCE_THRESHOLD_BASE=290`, `AGILITY_THRESHOLD_BASE=710`. Прогресс —
RAW урон/лечение без какой-либо нормализации по уровню: сила растёт от
`attackDamageDealt`, выносливость от `damageTaken`, ловкость от
`skillDamageDealt + healedAmount`. Копится в `strengthProgress`/
`enduranceProgress`/`agilityProgress` (Float), при пересечении порога — `stat++`,
остаток переносится (цикл `while`, может перепрыгнуть сразу несколько
уровней на один большой урон).
- Attack damage = 15 + Math.floor(strength / 2)

### Leveling — `calculateLevel`, `game.ts`
Уровень — ЧИСТАЯ функция текущих статов, никакой истории/снапшотов:
```
calculateLevel(strength, agility, endurance, bonusLevels):
  канал_урона     = floor((сила + ловкость - 20) / 6)
  канал_живучести = floor((выносливость - 10) / 3)
  уровень = 1 + max(канал_урона, канал_живучести) + bonusLevels
```
Два независимых канала (атака+ловкость vs выживание) — какой дальше ушёл,
тот и определяет стат-часть уровня. `bonusLevels` — счётчик уровней НЕ от
статов, только инкрементируется дискретными событиями (сейчас источник
один: убийство босса, +1). Колонка `Character.level` в БД осталась как
денормализованный снимок для аналитики/отладки — сама логика её НИГДЕ не
читает, только пишет (после каждого пересчёта статов).

Темп (по формуле, не с потолком): ~5 забегов до 2 уровня, ~23 до 100, ~28
до 500. Замедление плавное, жёсткого потолка нет.

---

## Economy
- Trophies = risky currency. Копятся МЕЖДУ забегами (не сгорают по одному
  забегу), стираются ТОЛЬКО смертью (весь банк целиком, см.
  `/run/finish-explore` в "Server Integration" ниже) — abandon по кнопке
  выхода тоже засчитывается как смерть, отдельного "тихого" выхода без
  потери нет.
- Gold = stable, used for potions (20 gold each). Обмен трофеев на золото —
  ТОЛЬКО между забегами (в меню), НЕ реализован (см. Next Steps,
  [ЭКОНОМИКА]). Единственный СУЩЕСТВУЮЩИЙ обмен трофеев — у Контрабандиста
  ВНУТРИ забега, ×1.5 при удаче / кража половины при провале (другая
  механика, не путать с обменом на золото).
- Potions: ПЯТЬ ТИРОВ, у каждого своя сила лечения и уровень открытия —
  каталог `src/potions.ts` (10/15/20/25/30% от maxHp, ур. 1/5/10/20/30, цена
  20/45/90/160/280). Запас свой на каждый тир (`Character.potionT1..T5`).
  `currentRun.potions` — снимок склада ПО ТИРАМ на старт забега,
  `currentRun.sips` — лимит глотков за забег (`MAX_SIPS_PER_RUN = 3`, но не
  больше суммы запаса). Пьётся всегда СТАРШИЙ доступный тир, кончился —
  следующий вниз прямо внутри забега; иконка на кнопке HUD показывает, что
  выпьется следующим.

---

## Next Steps (приоритет)

### Сделано (сессия 11-13.09.2026)

- **[ШИПЫ] Баг «урон от шипов сквозь платформу» ПОЧИНЕН.** Причина была НЕ в
  шипах: прямоугольник тела игрока (`PLAYER_HEIGHT = 128` от `phys.y`) выше
  настоящей макушки спрайта, а величина расхождения зависит от позы — в
  прыжке ноги поджаты и якорь смещён (`RISE_ANCHOR_Y = 0.729` /
  `FALL_ANCHOR_Y = 0.816`), поэтому фигура рисуется ниже. Замеры по альфе
  спрайтов: земля (idle/run) 3.73…18.86, воздух (кадры 9 и 14 `jump.png`)
  31.85…47.35 — разброс ~44px. Починка: `isTouchingSpikes` получает верх ТЕЛА
  вместо верха бокса, мерка выбирается по `phys.onGround` из двух ЗАМЕРЕННЫХ
  констант `SPIKE_BODY_TOP_GROUND = 3.73` / `SPIKE_BODY_TOP_AIR = 31.85`
  (минимум внутри группы поз, чтобы проверка не пропустила реальную голову).
  НИЗ остался `phys.y + PLAYER_HEIGHT` — шипы под ногами не затронуты.
  ⚠️ Держится на инварианте `JUMP_HIT_OFFSET_Y − TILE_SIZE*PLATFORM_H_RATIO −
  SPIKE_BODY_TOP_AIR <= 0` (сейчас −20.01), который перевернётся МОЛЧА при
  смене спрайта героя или высоты прыжка — см. одноимённую запись в Critical
  Gotchas и рецепт перезамера в комментарии к константам.

- **[ЗЕЛЬЯ] Система пяти тиров — ГОТОВА целиком** (схема, сервер, клиент,
  магазин, HUD):
  1. **Схема:** пять колонок `Character.potionT1..potionT5` вместо скалярного
     `potionCharges`. Две миграции — `20260911120000_potion_tiers_add`
     (ADD + `UPDATE potionT1 = potionCharges`) и
     `20260911130000_potion_charges_drop` (DROP отдельным заходом).
  2. **Единый каталог:** `src/potions.ts` — источник правды (тир, `healFrac`,
     `levelRequired`, `price`, иконка, название, флейвор) + `MAX_SIPS_PER_RUN`,
     `highestAvailableTier()`, `emptyPotionStock()`. Байт-в-байт копия
     `server/src/potions.ts`, сверка `python tools/check_potion_sync.py`
     (read-only, sha256) — гонять ПЕРЕД деплоем сервера, как `check_map_sync.py`.
     Общего пакета нет: `tsconfig.app.json` включает только `src`, у сервера
     `rootDir "./src"`. До каталога проценты лечения жили в ТРЁХ местах и
     противоречили друг другу (витрина обещала 10..30%, лечило всегда 25%).
  3. **Покупка:** `/character/buy-potion` принимает `{tier}`, цену и уровень
     открытия берёт из каталога, проверяет `calculateLevel(...)`, отдаёт
     `{gold, potions[5]}`. Кнопка в витрине подключена (была `onClick={() => {}}`).
  4. **Забег:** `/run/start-explore` кладёт в `currentRun` снимок склада по
     тирам и `sips`; клиент держит `potionStockRef`/`potionSipsLeftRef`, на
     кадре глотка выбирает `highestAvailableTier` и лечит
     `maxHp * POTION_TIERS[tier-1].healFrac`. Прежняя заглушка
     `POTION_HEAL_FRAC = 0.25` удалена.
  5. **Списание:** `/run/finish-explore` принимает `potionsDrunkByTier[]` и
     клэмпит в три ступени — поэлементная валидация (мусор → 0, как у
     `closedEvents`), потолок ПО КАЖДОМУ ТИРУ против `currentRun.potions`
     (именно он не даёт списать выпитое дорогое как дешёвое), потолок по сумме
     против `sips` со срезом излишка от МЛАДШИХ тиров вверх. Списывается
     независимо от `died`.
  6. **HUD:** кнопка зелья показывает фоном иконку тира, который выпьется
     следующим, и текстом число ОСТАВШИХСЯ ГЛОТКОВ; меняется по ходу забега.

### Открытые задачи (всплыли в сессии 11-13.09.2026)

- **[ТРОФЕИ, не воспроизводится] Сообщение «за успешный забег трофеи не
  прибавляются» НЕ подтвердилось, причина не найдена.** Цепочка прослежена
  целиком и оказалась целой. ПРОВЕРЕНО И ИСКЛЮЧЕНО: рассинхрон клиент/сервер
  по тройке событий (клиент берёт массив сервера как есть, индексы и длина
  совпадают); расхождение путей «смерть от скилла» vs «от меча» (обе идут
  через общую `damageEnemy`/`damageBoss`, путь закрытия события один); имя
  поля `trophyReward` (совпадает буква в букву с читающим его `trophySum`);
  длина массива событий (всегда 3, проверено прогоном реального
  `rollRunEvents` по всем семи картам). Порядок проверки, ЕСЛИ симптом
  вернётся: (1) была ли на экране итогов плашка `saveStatus` — красная «Забег
  не запомнят» или серая «Ты вне мира»; (2) есть ли `POST /run/finish-explore`
  в логах Render и с каким кодом (400 `No active explore run` и 401 ретраев
  не получают и уходят в `.catch` — только консоль и плашка); (3) как именно
  закончился забег — смерть / выход шестерёнкой / все три события. Без этих
  трёх фактов дальше копать бессмысленно.

- **[ПРОИЗВОДИТЕЛЬНОСТЬ, НЕ ИЗМЕРЕНО] Просадки/подвисания при беге на
  некоторых картах.** Похоже на догрузку текстур в момент появления объекта в
  кадре. Гипотеза НЕ проверена ничем — ни профайлером, ни замером, ни
  сопоставлением с конкретными картами/объектами. Не чинить по догадке:
  сначала измерить.

- **[ЗЕЛЬЯ, дыра] Закрыл приложение в забеге — выпитое НЕ списывается.**
  Прерванный забег закрывает `/auth/login` (обнуляет трофеи), но данных о
  выпитом нет — клиент их не прислал, а выдумывать нельзя (`auth.ts` отдаёт
  склад как есть). Следствие: убить приложение выгоднее, чем умереть, —
  прямое нарушение принятого правила «закрыть приложение никогда не должно
  быть выгоднее смерти» (см. Design Decisions, вариант А).

- **[ЗЕЛЬЯ/ЭКОНОМИКА] Покупка не ограничена сверху, а в забег едет максимум
  3 глотка.** `potionT<N>` копятся без потолка, `MAX_SIPS_PER_RUN = 3` —
  золото за зелья сверх этого тратится впустую и игроку об этом нигде не
  сказано.

- **[СНАРЯЖЕНИЕ] Экипировка на вкладке — фейковые данные.** `TEST_INVENTORY`
  (6 захардкоженных предметов) в `App.tsx`; реальный `GET /character/inventory`
  вкладкой НЕ вызывается. Зелья на той же вкладке переведены на настоящий
  `player.potions`, экипировка — нет.

### Открытые задачи (очередь, всплыли в Phase 2)
- **[EXPLORE, мелкое]** `enemiesRef` НЕ очищается в cleanup эффекта
  `Explore.tsx`, в отличие от `bossSpikesRef`/`bossWavesRef`/`rewardFloatsRef`
  (те обнуляются там же явно). Список врагов обнуляется ТОЛЬКО перед фазой
  спавна следующего забега (`enemiesRef.current = []` в `setup()`, до
  `chosenEvents.map(...)`), не на unmount. Всплыло при выносе AI врага в
  `entities/enemy.ts` — разобраться, нужна ли явная очистка на unmount, или
  текущее поведение осознанно (Pixi-узлы всё равно уничтожаются деревом
  `app.destroy(true, {children:true})`, реальной утечки может не быть).
- ~~**[БАГ, до релиза]** `setup()` в `Explore.tsx` вызывается без
  `.catch()`~~ — УСТАРЕЛО. `setup().catch(err => setSetupError(...))` есть,
  сетевой/asset-сбой уводит на экран ошибки (см. Phase 2.5 выше).
- ~~**[СЕРВЕР, Phase 2.5]** Новый Explore НЕ ходит на сервер... две точки
  входа не слиты~~ — ГОТОВО ПОЛНОСТЬЮ, см. "Server Integration — Explore
  (Phase 2.5)" выше: `/run/start-explore`/`/run/finish-explore` подключены,
  кнопка "Начать забег" ведёт в Explore (одна точка входа, debug-панель
  A-F/D — вторая, для тестов, осталась намеренно), тройка событий и её
  награды разыгрываются ТОЛЬКО сервером (см. "Единый источник тройки
  событий"), рост статов подключён и работает даже на смерти, `player`
  (trophies/strength/endurance/agility/level в App.tsx) обновляется СРАЗУ
  после реального ответа `/run/finish-explore` (`onRunComplete` →
  `handleExploreRunComplete`).
- **[КАРТЫ, не срочно]** Карта A проходится текущей физикой (прыжок ≈2.5
  тайла), но НЕ проходит строгую модель `report()` из `map_tools.py` (туда
  46/138) — местами требуется "вверх-3", который модель запрещает на
  обязательных путях. По факту играется нормально, решено оставить как есть.
  РИСК на будущее: если высоту прыжка снизят (баланс/другой персонаж/
  замедление), эти места станут тупиками — тогда перевести подъёмы на
  кладку/`stairs()`. Не блокирует Phase 2.
- **[КАРТЫ]** Ревизия проходимости всех шести карт под `JUMP_VELOCITY=11.5`
  — визуальная, численная валидация такие вещи не ловит (см. предупреждение
  в "Explore / Platforming Engine" выше).
- **[UI] Верхний HUD ГОТОВ** (см. "HUD-плита (HP + иконки событий)" и "Панель
  настроек" выше): единая плита `hp_frame_v2.png` (HP + 3 гнезда с реальными
  иконками событий, светящееся кольцо-индикатор завершения), шестерёнка
  открывает панель настроек (Продолжить/Звук/Музыка/Выйти+подтверждение)
  вместо прямого выхода. Осталось:
  1. **Реальное число трофеев** в диалоге подтверждения выхода — сейчас
     заглушка `0` (`// TODO` в коде `SettingsPanel.tsx`). `Explore` САМ уже
     получает `trophies` пропом (используется для клиентской прикидки
     итогов, см. `buildClientResult`) — недостающий шаг маленький: пробросить
     то же число ЕЩЁ и в `SettingsPanel`. Комментарий в коде объясняет
     причину неточно (говорит "Explore не получает trophies" — это больше не
     так), поправить заодно, когда будут трогать эту заглушку.
  2. **Стилизация кнопок панели настроек** — сейчас плоские прямоугольники в
     цветах дизайн-системы, не каменные/оформленные под остальной HUD-арт.
  Боевые кнопки (⚔/▲/dodge/◀▶) разложены и не входят в этот пункт.
- **[СПРАЙТЫ] Герой И зверь (обычный враг) ГОТОВЫ** в Explore.tsx (см.
  "Спрайты героя (idle/run/jump/attack/hurt/death)" и "Спрайты и боевая
  логика зверя (обычный враг)" выше — покадровая анимация, приоритет веток,
  poise/анти-стан-лок, death с задержкой закрытия события). Осталось одно:
  габариты хитбоксов игрок/враг под реальные размеры спрайтов — пока грубая
  прикидка прямоугольником, не пересматривали.
- **[ФИЗИКА/AI, мелкое]** Три вещи не доведены до конца в коллизии игрок↔враг
  (см. "Коллизия тел игрок↔враг" и "Прыжковый физический хитбокс" выше):
  1. Соскальзывание с макушки врага при падении точно на голову (падение вбок
     вместо повисания сверху) — НЕ реализовано, откладывали.
  2. Разворот врага при заходе игрока за спину (`playerInFront`) —
     реализован в коде, но НЕ подтверждён отдельным тестом на реальной карте.
  3. Блокировка атаки игрока в прыжке — предполагается, что ударить в
     воздухе нельзя (анимация прыжка перебивает attack-ветку раньше удара),
     но явной проверкой в коде это не защищено — не тестировали намеренно.

- **[ЭКОНОМИКА] Обмен трофеев на золото в меню** — блокирует экономику
  (см. "Система наград (Explore)"/"Economy" выше): золото в забегах больше не
  выпадает (убрано у сундука/обелиска), а магазин (зелья и т.п.) по-прежнему
  торгует на золото. Формула `rollGold` уже рассчитана (там же), но нигде
  не применена — сам обмен (UI + серверная логика начисления) не начат. Это
  обмен МЕЖДУ забегами (в меню) — не путать с Контрабандистом (обмен
  трофеев ×1.5/кража ВНУТРИ забега, уже реализован, другая механика).
- **[ПРЕДМЕТЫ] Система дропа предметов** — не начата, отложена до
  продумывания списка предметов: дроп с врага (шанс, ТОЛЬКО с последнего
  убитого в группе), с обелиска (шанс), с сундука (всегда, добрый исход).
- **[БОСС] Награда за победу — "сердце"** — переосмыслено: не автоматический
  попап выбора стата в момент убийства босса, а ПРЕДМЕТ в сумке (падает с
  босса ПОМИМО трофеев, `TROPHY_MULT_BOSS` уже реализовано), который игрок
  применяет САМ, когда захочет — применение даёт выбор стата (ловкость/
  выносливость/сила). Требует и дроп-систему (см. [ПРЕДМЕТЫ] выше), и
  серверную часть применения — не начато.
- **[ПРЕДМЕТЫ] Постоянный предмет брони на 15 уровне** — новый design note:
  предмет, применяемый один раз, НАВСЕГДА увеличивающий броню (тот же общий
  счётчик, что экипировка+заточка, см. "Броня в бою" выше). Не начато —
  ни предмета, ни механики "постоянного" применения нигде в коде нет.

- **[АРТ/КАРТЫ] Тематическая система карт ГОТОВА** (см. "Тематическая система
  карт" выше): 3 темы × (2 фона параллакса + атлас кладки + 6 декораций),
  привязка к картам через `BACKDROP_BY_MAP`. Осталось прибраться:
  1. ~~Удалить старые файлы~~ — ВЫПОЛНЕНО: `bg_classic_*`, `bg_mist_city_*`,
     `bg_overlook_*`, `bg_torchlit_*` (4 темы фонов), старый `masonry.png` и
     старые 18 декораций (torch/chain/banner/pillar/barrel/bones/grass/
     rubble/broken_pillar/broken_arch/chandelier/brazier/reliquary_altar/
     statue/hanging_cage/bookcase/sarcophagus/ritual_table) удалены одной
     уборкой — суммарно 34 файла ассетов, ~18 МБ (см. "[УБОРКА]" ниже).
  2. В `map_X_slots.json` декор разложен скриптом только для A и B (см.
     "Расстановка декора по картам" выше). Для C, D, E, F массива `decor`
     ещё нет — там рендер подставляет тематический декор детерминированно
     по координатам, что даёт объекты в воздухе. Разложить скриптом.

- **[УБОРКА] Аудит мёртвого кода/ассетов — ГОТОВО.** Удалено 34 файла
  ассетов (~18 МБ, см. "[АРТ/КАРТЫ]" выше) + мёртвый base64-остаток
  `SPRITES` (spikes/chest/obelisk) в `mapRenderer.ts` (см. "Рендер карты"
  выше). Проверено и подтверждено чистым: тюнеров с ползунками (`input
  type="range"`) в `src/` не осталось; мёртвых констант нет —
  `OBELISK_REWARD_GOLD_MIN/MAX` и `rollGold` в коде отсутствуют вообще
  (`rollGold` существует только как формула в этой документации, см.
  "Система наград (Explore)" выше); неиспользуемых импортов нет
  (`noUnusedLocals`/`noUnusedParameters` в tsconfig это гарантируют).
  Осталось по итогам того же аудита:
  1. **Сжать тяжёлые ассеты** — `hp_frame_v2.png` (3.5 МБ), иконки событий
     `event_*.png` (400–670 КБ каждая) — грузятся у игрока при каждом
     заходе на вкладку/забег.
  2. ~~`server/src/maps/` (14 файлов) не читается ни одной строкой
     серверного кода~~ — УСТАРЕЛО. Папка живая: 7 актуальных
     `map_*_slots.json` (копия `public/assets/maps/*_slots.json`), читает их
     `runEvents.ts` (`readSlots`/`MAPS_DIR`) для розыгрыша событий на сервере
     (Phase 2.5, см. ниже). Копия ручная — источник правды `public/assets/`,
     сверка `python tools/check_map_sync.py` (read-only, сравнивает по
     sha256) — гонять ПЕРЕД деплоем сервера, см. "Грабли" ниже.
- **[УБОРКА] Ассеты старого боевого потока — ГОТОВО.** Отдельная от пункта
  выше уборка (другой повод, старый поток, см. [СТАРЫЙ ПОТОК]): удалено 19
  файлов (старые заглушки `Idle.png`/`Walk.png`/`Attack_1.png`/`Hurt.png`/
  `bg-sky.png`/`bg-ruins.png`/`bg-floor.png`/`platform.png` в корне
  `public/assets/`, весь `public/assets/enemy/orc/` — 5 файлов, весь
  `public/assets/skills/` — 6 файлов). Перед удалением каждое имя
  прогонялось обратным поиском по всему репозиторию (не только `src/`) —
  ни одно не оказалось используемым за пределами уже удалённого кода.
  Папки `public/assets/enemy/` и `public/assets/skills/` удалены как
  опустевшие.

### Открытые задачи (всплыли при сверке docs/ с кодом)

Всё ниже найдено сверкой документации с кодом и осознанно НЕ чинилось на
месте — только зафиксировано.

- **[ПРЕДМЕТЫ, дыра в цикле] Получить предмет в игре НЕЛЬЗЯ ничем.** Дропа
  нет (функции `rollRandomItem`/`grantItem`/`dropChance` не существуют нигде
  в `server/src` — были привязаны к удалённым `/run/room`/
  `/run/battle-result`), магазин предметами не торгует (единственная покупка
  — `/character/buy-potion`, а зелья это счётчик `Character.potionCharges`,
  не `InventoryItem`). Единственное место в репозитории, где создаётся
  `InventoryItem` — ручной скрипт `server/prisma/debug-give-all-items.ts`.
  Следствия: `/character/inventory` и `/character/equip` рабочие, но
  обслуживают всегда пустой инвентарь; `totalArmor` у живого игрока всегда
  0, то есть ВСЯ ветка брони в бою (`takeDamage`, см. "Броня в бою") на
  реальном аккаунте не выполняется ни разу. Это блокирует и проверку брони,
  и смысл экипировки целиком — не деталь документации.
- **[ПРЕДМЕТЫ] Два несвязанных набора иконок, БД указывает на старый.**
  `Item.iconPath` резолвится в `public/assets/equipment/processed/<слот>/`
  — там арт СТАРОГО 10/5-тирного каталога (10 файлов на оружие), и его
  показывает экран "Персонаж". Набор под нынешние 6 тиров существует
  отдельно: `public/assets/icons/items/`, 36 файлов `wpn_t1`…`amu_t6`, и его
  использует только хардкодная вкладка "Снаряжение". Либо перегенерировать
  арт под `iconPath`, либо переписать `iconPath` в сиде на существующий
  набор — решить, но не молча.
- **[БОСС, мелкое] `BOSS_MAX_HP` и четыре `BOSS_*_DAMAGE` не читаются
  кодом.** Живые HP/урон считает `src/explore/scaling.ts` множителями поверх
  отмасштабированного обычного врага; эти пять констант остались справочными
  ("что формула даёт на 1 уровне") и уже разошлись с ней: формула даёт melee
  23 и melee2 33 (`ceil` от 22.1 и 32.3), а константы хранят 22 и 32. HP,
  шип и волна сходятся. Либо пересчитать, либо удалить как мёртвые.
- **[АССЕТЫ, мелкое] `Heal_Aura` и `Slash_Streak` грузятся без
  `assertSheetSize`** (`src/explore/assets.ts`) — единственные два из семи
  загружаемых листов скиллов без сверки размера. Проверка громкая и дешёвая,
  добавить нечему помешать.
- **[SKILLS] Тип `equipped` не сужен до `SkillId`.** В `SkillsDeps` он
  `[string | null, string | null]`; комментарий у поля обещает сузить "вместе
  с реализацией последнего из пяти" — условие выполнено, сужение не сделано.
  Вместе с ним уйдёт рантайм-список `KNOWN_SKILL_IDS`, нужный сейчас только
  для диагностики.
- **[SKILLS] Устаревший комментарий в коде**: `Explore.tsx:3199-3200`, прямо
  над вызовом `skills!.update(...)`, до сих пор утверждает "пока только
  гасит skill1Pressed/skill2Pressed, самой логики ещё нет". Последний
  остаток формулировки времён пустого каркаса.
- **[UI/SKILLS] Индикатора кулдауна на кнопках скиллов НЕТ ни у одного из
  пяти.** Кулдауны тикают внутри модуля и наружу не отдаются: кнопка
  показывает только "слот пуст / слот занят / профиль не загружен"
  (`updateSkillButtons` в `Explore.tsx`). Игрок не видит, сколько осталось до
  готовности — при 5с у четырёх скиллов и 10с у slash это заметно.
- **[SKILLS] Ловкость не влияет на урон скиллов** (перенесено из удалённого
  п.7): решено, что должна — не только разблокировка и рост ловкости ОТ
  использования, но и обратная связь ловкость→урон. Формулы нет. Придёт
  вместе с общей формулой урона скиллов, которая заменит четыре временных
  доли maxHp сразу (см. `docs/skills.md`).

### Открытые задачи (Phase 2.5)
- **[EXPLORE] Загрузка параллакс-фона не обёрнута в try/catch.**
  `Assets.load(farUrl)`/`Assets.load(midUrl)` в `setup()` (`Explore.tsx`) —
  404 на любом из двух PNG обрывает ВЕСЬ `setup()`, игрок видит экран
  ошибки вместо игры без фона (см. "Грабли текстур карты" выше — там эта
  запись раньше ошибочно утверждала, что try/catch уже есть).
- **[БАГ] Энергия упирается в 99, до 100 не доходит.** Наблюдается вживую.
  Подозреваемое место — `getCurrentEnergy` (`server/src/game.ts`):
  `Math.floor((Date.now()-lastUpdate)/60000)` теряет дробный остаток минуты
  при каждом обращении, а `lastEnergyUpdate` в БД сдвигается только при
  трате энергии (старт забега), не при простом чтении — похоже, что именно
  здесь теряется последняя единица, но причина НЕ подтверждена отладкой,
  только по коду.
- ~~**[СТАТЫ] Рост статов НЕ работает в Explore.**~~ — ГОТОВО, см. "Server
  Integration", `POST /run/finish-explore`: `applyStatGrowth` подключён,
  читает `attackDamageDealt`/`skillDamageDealt`/`healedAmount`/`damageTaken`
  из тела запроса (клэмпнутые анти-читом), растит статы даже на смерти.
- ~~**[СЕРВЕР] Старые эндпоинты не проверяют формат `currentRun`.**~~ —
  СНЯТО: эндпоинты, о которых шла речь (`/run/room`/`/run/battle-result`/
  `/run/smuggler-result`/`/run/puzzle`/`/run/puzzle-result`), удалены
  вместе со старым потоком (см. пункт ниже) — риск исчез вместе с кодом.
- ~~**[СТАРЫЙ ПОТОК] Удалить `Battle.tsx`/`Smuggler.tsx`/`Puzzle.tsx`/
  `/run/start`/`/run/room`/`/run/battle-result`/`/run/puzzle*`/
  `/run/smuggler-result`/`puzzles.ts`**~~ — ВЫПОЛНЕНО: удалено тремя
  коммитами (фронт → сервер → ассеты), задеплоено и проверено живым
  забегом в Telegram. Старого 3-комнатного потока в проекте больше нет.
- **[БЕЗОПАСНОСТЬ] Контрабандист: исход обмена сообщает клиент, сервер
  верит на слово.** `/run/finish-explore` берёт `smugglerOutcome` из тела
  запроса как есть и применяет `SMUGGLER_MULT`/`SMUGGLER_STEAL_FRAC` без
  переигровки броска — известное слабое место (клиент теоретически может
  всегда слать `'gain'`).

1. ~~**[СЕРВЕР, Phase 2.5] Серверная интеграция Explore**~~ — ГОТОВО
   ПОЛНОСТЬЮ, см. "Server Integration — Explore (Phase 2.5)" выше:
   `/run/start-explore`/`/run/finish-explore` подключены, смерть от hp<=0
   идёт через results-экран (не мимо через abandon), одна точка входа
   ("Начать забег"), тройка событий и её награды разыгрываются ТОЛЬКО
   сервером (см. "Единый источник тройки событий" там же), рост статов
   подключён (растёт и на смерти), `player` в App.tsx обновляется сразу
   после ответа `/run/finish-explore`. Открытых остатков по интеграции
   Explore⇄сервер не осталось — оставшиеся [ЭКОНОМИКА]/[БЕЗОПАСНОСТЬ]/
   [ПРЕДМЕТЫ]-пункты выше — отдельные, не блокирующие задачи.
2. ~~**Интеграция спрайтов героя и зверя в Battle.tsx**~~ — ОТПАЛО:
   `Battle.tsx` удалён вместе со старым потоком (см. [СТАРЫЙ ПОТОК] выше),
   подключать спрайты стало некуда. Герой и зверь уже интегрированы в
   `Explore.tsx` (см. "Спрайты героя"/"Спрайты и боевая логика зверя" выше).
3. ~~Механика обелисков (F)~~ — ГОТОВО, подключено в Explore.tsx (см. "Готовые
   ассеты — Загадка-обелиск" выше: рулетка событий, таймер с добавкой за удар,
   награда, closeEvent, смерть по таймеру).
4. ~~Механика tainik_seal (D)~~ — ГОТОВО (см. "Особая механика: тайник карты
   D" выше): геометрия OPEN/SEALED 50/50 + гарантия "OPEN→Контрабандист"
   реализованы.
5. ~~Масштабирование врагов~~ — ГОТОВО (`src/explore/scaling.ts`). Старый план
   "+10% HP / +8% урон за уровень" (был записан здесь и в `pixijs-conventions`
   скилле) ОТМЕНЁН — компаунд-рост давал ~1.5 млн HP на 100 уровне. Вместо
   него — ЛИНЕЙНАЯ формула, считается ОДИН раз при спавне и кладётся в поля
   сущности (`Enemy.attackDamage`, `Boss.maxHp/meleeDamage/melee2Damage/
   spikeDamage/waveDamage`), не пересчитывается на лету и не задваивается со
   стат-нормализацией на сервере (та считается по СТАТАМ игрока в старом
   потоке, эта — по врагам в Explore, разные механизмы):
   `HP врага = round(ENEMY_MAX_HP + ENEMY_HP_PER_LEVEL*(lvl-1))` = `round(100
   + 9.0*(lvl-1))`. Урон — round ЗАМЕНЁН НА `Math.ceil` (см. задачу "HP
   должно быть всегда целым"/"Броня в бою" выше — округление урона никогда
   не должно идти в пользу игрока): `урон врага = ceil(ENEMY_ATTACK_DAMAGE +
   ENEMY_DAMAGE_PER_LEVEL*(lvl-1))` = `ceil(17 + 4.7*(lvl-1))`. Босс — НЕ
   отдельная формула, а множители поверх уже отмасштабированного врага на том
   же уровне (тоже `ceil` для урона, `round` для HP): HP ×`BOSS_HP_MULT`(2.5),
   melee ×1.3, melee2 ×1.9, шип ×1.0, волна ×1.15. Старые константы
   (`ENEMY_MAX_HP`/`ENEMY_ATTACK_DAMAGE`/`BOSS_MAX_HP`/`BOSS_MELEE_DAMAGE`/
   `BOSS_MELEE2_DAMAGE`/`BOSS_RANGED_DAMAGE`/`BOSS_WAVE_DAMAGE`) не удалены —
   стали значениями формулы НА УРОВНЕ 1 (100/17/250/22/32/17/20), у ENEMY_*
   дополнительно роль базы формулы, у BOSS_* — только справочная (живой
   урон/HP босса больше не читает их напрямую, см. `boss.ts`). Уровень
   персонажа читается из `characterLevelRef` (см. п. выше про проброс
   уровня) РОВНО ОДИН раз в момент спавна — уже заспавненный враг/босс не
   меняет HP/урон, даже если уровень посреди забега почему-то изменится.
   ⚠️ **HP-формула (`scaledEnemyMaxHp`/`scaledBossMaxHp`) ПРОДУБЛИРОВАНА в
   `server/src/game.ts`** (используется для anti-cheat потолка урона в
   `/run/finish-explore`, см. "Server Integration" выше) — общего пакета
   между клиентом и сервером в проекте нет, синк РУКАМИ: если
   `ENEMY_MAX_HP`/`ENEMY_HP_PER_LEVEL`/`BOSS_HP_MULT` меняются в
   `src/explore/scaling.ts`+`constants.ts`, те же числа надо поменять в
   `game.ts`, иначе потолок урона молча разъедется с тем, что реально
   заспавнено в игре.
6. **Equipment** — 6 слотов, тиры каждые 5 уровней. Иконки пустых слотов
   готовы, иконки реальных предметов — нет. Плюс полировка UI (сейчас плоские
   заглушки, не под общий каменный стиль HUD-арта).
7. ~~Boss — AI/бой/анимации/награда~~ — ГОТОВО: весь набор
   (Idle/Walk/Melee/Melee2/Ranged/Stomp/Hurt/Death + шип/импакт/AoE-волна)
   подключён к `Explore.tsx`, стадии/poise/дальний бой/топот реализованы,
   босс гарантирован в рулетке событий, награда трофеями + закрытие события
   по концу death-анимации (см. "Босс (карта C) — ФАЗА 2..5" выше). Осталось
   — см. **[БОСС]** в открытых задачах выше: "сердце" (предмет-награда за
   победу, применяется игроком, даёт выбор стата) — требует дроп-систему и
   серверную часть, не начато.
8. ~~Сундук~~ — ГОТОВО (открытие атакой + мимик + reward float, см. "Chest
    (Explore)" выше). ~~Питьё зелья (drink)~~ — ГОТОВО (хил/заряды/кулдаун,
    см. "Potion (Explore)" выше). ~~Контрабандист~~ — ГОТОВО (окно обмена,
    см. "Готовые ассеты — Контрабандист" выше). ~~Обелиск~~ — ГОТОВО (рулетка
    событий + таймер + награда, см. "Готовые ассеты — Загадка-обелиск" выше).
9. **УБРАТЬ ПЕРЕД РЕЛИЗОМ** (сведено в один чеклист — раньше было
    разрозненно по файлу, легко забыть один пункт):
    - **`RUN_COST=3`** в `server/src/routes/run.ts` (Critical Gotchas выше)
      — вернуть на 10.
    - **Временный переключатель карт A–F** (`// TEMP: map switcher` в
      `Explore.tsx`, панель настроек) — осознанно ОТЛОЖЕНО, не баг: нужен
      для проверки фонов параллакса на всех картах.
    - **Тумблер debug-бессмертия** в `SettingsPanel` (`invincible` state в
      `Explore.tsx`, см. "Броня в бою" выше) — тестовый режим, перехватывает
      весь урон в `takeDamage` до применения брони; `killPlayer()`
      (гарантированная смерть обелиска) его ТОЖЕ уважает намеренно.
    - **`TEMP_DEV_SKILLS`** в `App.tsx` (помечен этим маркером в комментарии,
      сейчас `equippedSkills: ['iceball', 'fireball']`) — временный набор под
      локальную проверку двух снарядных скиллов рядом. Влияет ТОЛЬКО на
      офлайн-заглушку `DevTester` (вне Telegram); в Telegram скиллы приходят
      с сервера и этой строкой не задеваются. Перед релизом вернуть боевой
      набор — по прежней записи в коде это было `['heal', 'dash']`.
    - **Панель диагностики прыжка** (`// TEMP DEBUG: jump diagnostics`) —
      временный замер пропавших прыжков: панель под HP-плитой и счётчики в
      `Explore.tsx`, счётчик нажатий ▲ в `TouchControls.tsx`. Снимать целиком:
      после уборки `grep -rn "TEMP DEBUG: jump diagnostics" src` должен быть
      пуст, а кнопка ▲ в `TouchControls.tsx` вернуться к одной строке
      `bindTap(jump, () => { jumpPressedRef.current = true })`.
    - Панели тюнеров (`TEMP_FIREBALL_TUNER`/`TEMP_ICEBALL_TUNER`) в этом
      чеклисте НЕ значатся и не нужны — удалены вместе с запеканием чисел
      снаряда в константы, `grep -rn "TEMP_FIREBALL\|TEMP_ICEBALL" src` пуст.
10. **[ПРОВЕРИТЬ ИГРОЙ]** Новая прокачка/трофейная экономика (см. "Stat &
    Leveling System"/"Система наград" выше) пока проверена только по
    формулам, не вживую за реальные забеги:
    - темп роста статов/уровня на практике совпадает с расчётным (~5
      забегов до 2 уровня и т.д.) — не только на бумаге;
    - не выгодно ли умирать нарочно (трофеи горят при смерти целиком, но
      рост статов/уровня от урона за забег НЕ откатывается — нужно
      убедиться, что суммарно смерть всё ещё строго хуже, чем не умирать);
    - не уезжает ли `bonusLevels` (от частых убийств босса) далеко вперёд
      стат-уровня — герой формально высокого уровня с непропорционально
      слабыми статами/уроном.

### Game Mechanics chapter — приоритет (обновлено)
[DONE] #1 сундук (открытие + мимик), #2 попап награды (reward float), #3
зелье (хил/заряды/кулдаун) — см. "Chest (Explore)" / "Reward float
(Explore)" / "Potion (Explore)" выше.
[DONE] #4 Контрабандист (переиспользует reward float, см. "Готовые ассеты —
Контрабандист" выше) — окно обмена, 80/20 ×1.5/кража, оба исхода закрывают
событие.
[DONE] #7 обелиски карты F (см. "Готовые ассеты — Загадка-обелиск" выше) —
рулетка событий, таймер с добавкой за удар, доспавн 3 после первого удара,
награда + closeEvent на успех, смерть по таймеру на провал.
[DONE] #8 tainik_seal карты D — геометрия OPEN/SEALED 50/50 + связка
"OPEN→Контрабандист гарантирован" реализованы (см. "Особая механика: тайник
карты D" выше).
Осталось по порядку: #5 рассеянный спавн врагов, #6 масштаб врагов по уровню
(дублирует п.5 выше).
Чеклист "убрать перед релизом" (RUN_COST и др.) — см. п.9 выше, не забыть.

---

## Design Decisions
- Puzzle = game-mechanics quiz
- Heal/potions don't affect Endurance growth (totalDamageTaken tracked separately)
- actualHpLost sent to server for correct HP carry between rooms
- Enemy base stats: normal 120HP/14dmg, boss 200HP/18dmg
- Wide arena (3000px) with camera follow
- Флип по направлению в Explore ГОТОВ и разный для героя/зверя (проверено на
  реальных ассетах, не общее правило "sprites face left by default"): герой
  смотрит ВПРАВО по умолчанию (facing===1 → без зеркала, facing===-1 →
  scale.x в минус), зверь смотрит ВЛЕВО по умолчанию (facing===-1 → без
  зеркала, facing===1 → scale.x в минус) — см. "Спрайты героя"/"Спрайты и
  боевая логика зверя" выше. В Battle.tsx (старая боевая сцена) этот флип
  ещё не переносился — там актуальность прежнего допущения не проверялась.
- **Тихие фолбэки — запрещены, делать их громкими.** `?? 10`,
  `if (!x) return` и подобное молча подставляют правдоподобное, но ложное
  значение вместо явного сигнала "данных нет" — баг маскируется, а не
  чинится, и стоит времени на отладку (реальный случай — фолбэк на
  экране "Персонаж", который тихо показывал `10` вместо настоящей брони,
  когда `inventory` ещё не успел загрузиться; чинили explicit guard'ом
  "данные недоступны" вместо тихого числа). Тот же принцип — за фолбэком
  без токена в Explore (см. "Единый источник тройки событий" выше):
  оффлайн-заглушка розыгрыша событий ОСТАВЛЕНА (нужна для UI-отладки без
  Telegram), но помечена ВИДИМОЙ оранжевой плашкой на экране, а не тихо
  подменяет данные неотличимо от настоящего забега.
- **Обрыв связи в забеге — вариант А, ПРИНЯТО.** Свернул приложение — игра
  замирает в памяти и продолжается сама при возврате, ничего специально
  делать не надо (проверено вживую). Закрыл приложение полностью — забег
  закрывается как смерть: при следующем входе показывается экран итогов,
  трофеи сгорают. Никакого промежуточного сохранения состояния забега НЕ
  делаем. Причина отказа: любое сохранение, которое откатывает игрока
  назад при переоткрытии, делает закрытие приложения выгоднее смерти
  (закрыл перед боссом на низком HP — получил бесплатный ретрай). Правило
  на будущее: закрыть приложение никогда не должно быть выгоднее, чем
  умереть в забеге. Плюс варианта А — сам момент закрытия ловить не нужно
  вообще: сервер видит открытый `currentRun` при следующем входе и
  закрывает его сам.
