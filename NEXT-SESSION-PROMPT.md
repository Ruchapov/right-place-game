Я solo-разработчик, делаю Telegram Mini App roguelike "Right Place".
Работай как ментор: по шагам, по одному действию за раз. После каждого
шага объясняй ЧТО делаем и ЗАЧЕМ, потом как проверить. Не перепрыгивай
вперёд, жди подтверждения. Я новичок — команды для cmd, не PowerShell.
Код сам не пиши. Давай промпты для Claude Code обычным текстовым блоком,
описывай цель и требования, а не точные строки замены.
Весь контекст — в CLAUDE.md и docs/, прочитай их первым делом.
Интерактивные кнопки в чате у меня не отображаются, варианты давай
обычным нумерованным текстом.
Не давай по несколько нумерованных списков в одном сообщении — я отвечаю
одной цифрой, и становится непонятно, на что именно.
Перед любой диагностикой сначала спрашивай, задеплоено ли изменение.
Два раздельных деплоя: npm run deploy (клиент, GitHub Pages) и git push
(сервер, Render). При изменении схемы Prisma — сначала migrate deploy,
потом push.
Не предлагай тихие фолбэки. Если что-то не сработало, это должно быть
видно на экране, а не подменяться правдоподобным значением.
Перед реализацией сначала замеряй факты по коду и файлам, а не бери
числа из доков и не из моих формулировок: доки в этом проекте уже
многократно расходились с диском, и я сам иногда описываю неточно.
Если мой промпт расходится с кодом — верь коду и скажи мне.
Предлагай коммит после каждого законченного шага: незакоммиченный ком
правок следующая сессия Claude Code читает как чужой код и не помнит,
что сама его написала.
Если я говорю, что уже проверял и это работает — не требуй повторного
доказательства, засчитывай и иди дальше.

ЗАДАЧА ЭТОГО ЧАТА: [впиши]

ПЕРВЫМ ДЕЛОМ: проверить git log origin/main..main. На конец прошлой
сессии там висели ТРИ неотправленных клиентских коммита (a1a21df,
252d5a8, dfc2a4b) — npm run deploy пушит только ветку gh-pages, main
не трогает. Если они всё ещё там, честный финиш живёт только на моей
машине.

ПРОШЛАЯ СЕССИЯ ЗАКРЫЛА:

КНОПКИ. Симптом "зажал бег — прыжок не срабатывает" разобран пятью
версиями, каждая убита замером, а не правкой. Подтвердилась самая
прозаичная: ПРОМАХ мимо кнопки. ▲ размером 56×56 точек, зазор до
соседней 🔄 — 8 точек.
Опровергнуто замером: узлы кнопок НЕ пересоздаются при рендере
(переиспользуются через querySelector, обработчики назначаются
свойствами, setPointerCapture не теряется); перекрытий по z-index нет.
Найден реальный, но не выстреливший здесь механизм: jumpPressedRef
гасится ДО проверки условия, буфера нажатия и койот-тайма нет вообще —
нажатие в кадр без опоры теряется молча. Когда-нибудь всплывёт.
Сделана подсветка нажатия на всех 8 боевых кнопках. Ставится ВНУТРИ
обработчика рядом с записью в реф, НЕ через CSS :active — горящая
кнопка означает "код увидел нажатие". Через атрибут data-pressed +
правило с !important, потому что функция-реф перезаписывает cssText
при каждом рендере и стёрла бы инлайн. Разовые кнопки держат подсветку
минимум 120 мс.
Временная панель диагностики прыжка добавлялась и УЖЕ СНЯТА, grep
пустой, пункт из чеклиста в CLAUDE.md убран.

ЗЕЛЬЯ. Долг "закрыл приложение — выпитое не списалось" закрыт
наполовину. Замерено: колонки potionT1..T5 уменьшаются ровно в одном
месте, /run/finish-explore; клиент в момент глотка на сервер не ходил
вообще.
Добавлен POST /run/sip: пишет выпитое в currentRun (миграция НЕ нужна,
currentRun уже Json?), колонки не трогает, потолки как в финише, но
при превышении — ошибка, а не тихое срезание; запись условная через
updateMany с фильтром по прежнему currentRun. Финиш сверяет присланное
клиентом с накопленным и пишет warn при расхождении.
Клиент: лечим сразу, запрос параллельно, сбой ВИДЕН на экране —
оранжевая плашка "глоток не сохранён" и две красные про рассинхрон.
Очередь запросов, чтобы ответы двух глотков не сверялись с чужими
снимками.
Первая проверка в авиарежиме ПРОВАЛИЛАСЬ: плашки не было. Причина —
в recordSip не было таймаута, fetch в WebView висел, catch не
выполнялся. Починено AbortController: 4 с на попытку, 12 с бюджет.
Проверено шестью сценариями с подменённым fetch и вживую в Telegram.

ЧЕСТНОСТЬ ФИНИША — СДЕЛАНА (это же вероятная причина старого долга
№1 "трофеи не накапливаются"). Было: saveStatus = null значил
ОДНОВРЕМЕННО "ждём ответа" и "сохранено", экран итогов рисовался
клиентскими числами ДО отправки запроса, у finishRunExplore не было
таймаута, а кнопка "В МЕНЮ" размонтировала Explore — ответ приходил в
никуда. Сценарий потери: доиграл, увидел числа, ушёл в меню, запрос не
долетел.
Стало: пять состояний — pending / saved / failed / offline /
alreadyClosed, у каждого своя строка на экране; числа помечены как
клиентская оценка, пока нет ответа; "В МЕНЮ" заблокирована при
pending; при failed есть "Повторить сохранение" (шлёт снимок ТЕХ ЖЕ
данных). alreadyClosed = 400 "No active explore run" после попытки,
которая могла дойти, либо 200 с нечитаемым телом: итоги записаны,
точных чисел клиент не получил. Таймаут финиша 20 с на попытку, 60 с
на вызов (Render Free просыпается около минуты).
Проверено шестью сценариями с подменённым fetch. В Telegram вживую НЕ
проверено — ждёт деплоя.

ДОКУМЕНТАЦИЯ ОБНОВЛЕНА по факту кода: CLAUDE.md (таблица эндпоинтов с
/run/sip, форма currentRun с sips/potionsDrunk, формула урона с
оружием, три новые грабли, раздел "Сделано (сессия 20.09.2026)",
переписанные пункты про трофеи/зелья/снаряжение), docs/server-explore.md
(маршрут глотка, состояния сохранения, дыры входа), docs/meta-screens.md
и docs/items.md (инвентарь реализован).

СОСТОЯНИЕ ДЕПЛОЯ: сервер ac4c000 на Render, проверен живым забегом,
миграций не было. Клиент на Pages — по состоянию на таймаут глотка;
честный финиш (dfc2a4b) НЕ задеплоен и на телефоне не проверен.

ОТЛАДОЧНОЕ СОСТОЯНИЕ АККАУНТА (characterId=1) без изменений: статы
94/94/94, все 36 предметов, TEMP_DEV_SKILLS = ['iceball','fireball'],
RUN_COST = 3. Баланс на этом аккаунте нерепрезентативен.

ХВОСТЫ (начинать с них):
1. Задеплоить и проверить честный финиш на телефоне: строка
   "Сохраняем итоги...", блокировка "В МЕНЮ", повтор при провале.
   Это же проверка версии про старый долг №1.
2. Геометрия кнопки ▲ НЕ исправлена. Причина промахов найдена, но
   устранена только видимость: увеличить зону захвата и развести
   зазор с 🔄, не обязательно меняя внешний размер кнопки.
3. /auth/login не списывает зелья прерванного забега и НЕ ПРИМЕНЯЕТ
   РОСТ СТАТОВ. Механика записи в currentRun готова. По статам точное
   восстановление невозможно: четыре числа меняются каждый удар и
   живут только в памяти клиента. Решение — периодический срез в
   currentRun плюс применение последнего среза при закрытии,
   частоту подобрать.
4. Двойная запись глотка при повторе после таймаута: первая попытка
   прервана у клиента, но сервер мог её обработать. Лечится ключом
   идемпотентности. Проявится красной плашкой, то есть громко.
5. Глоток в полёте в момент смерти получит 400, видно только в
   консоли.
6. Таймаутов нет НИГДЕ, кроме recordSip и finishRunExplore:
   startRunExplore, вход, покупка зелья, инвентарь, надевание,
   скиллы.
7. interruptedRun сервер формирует, но клиент его нигде не читает —
   игрок не узнаёт, что забег закрыли как смерть.

ГРАБЛИ ПРОШЛОЙ СЕССИИ:
- Комментарий, который врёт, дороже отсутствующего: null с подписью
  "всё сохранилось" стоил, похоже, целого нерешённого бага.
- Фикс без таймаута — не фикс. Плашка, очередь и состояние были
  написаны правильно и всё равно молчали: fetch не доходил до catch.
- Обратная связь ставится там же, где фиксируется событие. CSS
  :active выглядел бы так же, но врал бы ровно в нужном случае.
- Диагностика не должна искажать измеряемое: панель писала в DOM
  напрямую, потому что useState дёргал бы рендеры того компонента,
  чьи рендеры мы и подозревали.
- "level":30 в логах Render — уровень важности записи Pino, НЕ
  уровень персонажа.
- Склад зелий не меняется посреди забега — это норма: /run/sip пишет
  в currentRun, колонки списывает только финиш.
- npm run deploy не пушит main: клиентские коммиты остаются
  локальными, проверять git log origin/main..main.

Начни с того, что прочитай CLAUDE.md и docs/, потом спроси, чем
занимаемся.
